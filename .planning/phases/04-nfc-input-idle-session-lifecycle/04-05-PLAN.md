---
phase: 04-nfc-input-idle-session-lifecycle
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified:
  - test/sessionReset.harness.js
  - test/phase4-integration.test.js
  - .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md
autonomous: false
requirements: [NFC-01, NFC-02, NFC-03, NFC-04, NFC-05, NFC-06, IDLE-01, IDLE-02, IDLE-03, IDLE-04, IDLE-05, IDLE-06, IDLE-07]
tags: [verification, harness, acceptance, human-checkpoint]

must_haves:
  truths:
    - "100-cycle hardReset harness runs 100 consecutive sessionReset.hardReset() calls against mocked storage/view and proves no mutex violation, no residual state, no half-logged-in cycles (IDLE-04)"
    - "Phase 4 integration test suite proves: start-idle-timer side-effect on CASH_REGISTER_READY transitions both branches; magiclineView KNOWN_EVENT_TYPES whitelist contains the 3 new entries; handleInjectEvent delegations forward correctly"
    - "Human checkpoint on real kiosk hardware validates: first-scan-after-boot captures leading char (NFC-03), idle overlay appears after 60s, countdown dismisses on tap, expired countdown triggers clean reset, 'Jetzt verkaufen' clears customer field 3s later, product-search focus pass-through works, simulated render crash recovers, >3 resets in 60s shows reset-loop error, admin PIN on reset-loop triggers app.relaunch"
  artifacts:
    - path: "test/sessionReset.harness.js"
      provides: "100-cycle mutex+loop-counter harness"
    - path: "test/phase4-integration.test.js"
      provides: "Integration tests exercising authFlow → idleTimer → sessionReset wiring"
    - path: ".planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md"
      provides: "Human verification checklist with pass/fail capture per requirement"
  key_links:
    - from: "harness"
      to: "sessionReset.hardReset 100x"
      via: "loop with mocked electron + magiclineView + idleTimer"
      pattern: "100"
    - from: "human checkpoint"
      to: "physical kiosk"
      via: "actual Deka reader scans + touch + crash simulation"
      pattern: "kiosk"
---

<objective>
Prove Phase 4 meets all 13 requirements (NFC-01..06 + IDLE-01..07) through three verification artifacts:

1. A 100-cycle automated harness that drives sessionReset.hardReset() 100 times in a row (IDLE-04 requirement text literal) and asserts the mutex never leaks, the rolling window behaves correctly, and all 100 cycles complete the D-15 step sequence in order.
2. An integration test suite that exercises the cross-module wiring: authFlow → idleTimer.start, idleTimer.expired → sessionReset.hardReset, magiclineView drain → badgeInput/idleTimer.
3. A human checkpoint against the physical kiosk hardware that validates every requirement with actual NFC scans, touches, crash simulations, and the reset-loop path.

Purpose: This is the IDLE-04 acceptance artifact (the 100-cycle harness is REQUIREMENT-SPEC text, not optional). It is also the first and only place in Phase 4 where a human actually holds a badge to the real reader — all prior plans use mocks or contract assertions.
Output: Two new test files (automated), one verification document (human-filled), and a final phase summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-nfc-input-idle-session-lifecycle/04-CONTEXT.md
@.planning/phases/04-nfc-input-idle-session-lifecycle/04-RESEARCH.md
@.planning/phases/04-nfc-input-idle-session-lifecycle/04-01-PLAN.md
@.planning/phases/04-nfc-input-idle-session-lifecycle/04-02-PLAN.md
@.planning/phases/04-nfc-input-idle-session-lifecycle/04-03-PLAN.md
@.planning/phases/04-nfc-input-idle-session-lifecycle/04-04-PLAN.md
@src/main/sessionReset.js
@src/main/idleTimer.js
@src/main/badgeInput.js
@src/main/magiclineView.js
@src/main/authFlow.js
@test/sessionReset.test.js

<interfaces>
The 6 Phase 4 success criteria from ROADMAP.md that must ALL be observable after this plan:

1. A member scanning their Deka badge sees their name appear in the Magicline customer field within one second, on the very first scan after boot (no dropped first character) and on every subsequent scan.
2. While staff have the Magicline product-search field focused, product barcode scans type into that field normally and are not hijacked into the customer-search buffer.
3. After 60 seconds of no activity, a branded "Are you still there?" overlay appears with a visible 30s countdown; any input dismisses and returns the same cart.
4. Overlay expiry wipes the Magicline session completely and returns to a clean cash register. 100 repeated reset cycles never produce a half-logged-in state.
5. Clicking "Jetzt verkaufen" clears the customer search field 3 seconds later while keeping the sale in history. A render crash auto-recovers into splash then clean re-login.
6. More than 3 hard resets within 60 seconds stops the loop and shows a branded error screen (reset-loop variant).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 100-cycle sessionReset harness (IDLE-04 acceptance artifact)</name>
  <files>test/sessionReset.harness.js</files>
  <read_first>
    - src/main/sessionReset.js (the module under test)
    - test/sessionReset.test.js (mocking patterns for electron, magiclineView, idleTimer)
    - .planning/phases/04-nfc-input-idle-session-lifecycle/04-CONTEXT.md (D-14 through D-20)
    - .planning/phases/04-nfc-input-idle-session-lifecycle/04-RESEARCH.md (Pattern 4 step order, Pattern 6 rolling window, Assumption A5 flushStore ordering)
  </read_first>
  <action>
    Create test/sessionReset.harness.js using node:test. This is a standalone harness — it lives alongside regular unit tests but the test case count is the load-bearing assertion.

    Structure:

    1. Setup (shared across all test cases in this file):
       - Mock `electron` via require.cache: provide session.fromPartition returning an object whose clearStorageData and cookies.flushStore are async stubs that push call-log entries and optionally simulate a delay (random 0-5ms per call to stress-test ordering).
       - Mock `./magiclineView` with destroyMagiclineView and createMagiclineView stubs that push to the same call-log and increment counters.
       - Mock `./idleTimer` with a stop() stub.
       - Mock mainWindow with a webContents.send stub that records channel names.
       - sessionReset.init({mainWindow, store: null}) before each cycle.

    2. Test case 'harness: 100 consecutive hardReset cycles complete D-15 step order':
       - Loop i from 0 to 99:
         a. Reset the call-log array
         b. Call `await sessionReset.hardReset({ reason: 'idle-expired' })`
         c. Advance the rolling window by 70 seconds using t.mock.timers.tick(70_000) so the counter does not trip the loop guard (since we are testing non-loop behavior here; separate test covers the loop)
         d. Assert call-log contains in ORDER: ['idleTimer.stop'], ['ipc', 'splash:show'], ['destroyMagiclineView'], ['clearStorageData', {storages: [6 exact types]}], ['flushStore'], ['createMagiclineView']
         e. Assert sessionReset._getStateForTests().resetting === false after each cycle
         f. Assert sessionReset._getStateForTests().loopActive === false
       - After the loop, assert the loop completed 100 cycles without throwing and without any cycle leaving resetting=true.

    3. Test case 'harness: concurrent hardReset calls during in-flight are suppressed 100x':
       - Make clearStorageData return a promise that resolves after N ticks.
       - For i in 0..99, call hardReset() twice concurrently without awaiting; the second call in each pair must log sessionReset.suppressed: in-flight.
       - Assert the call-log destroy+create counters each equal 100 (one per pair, not two).

    4. Test case 'harness: reset-loop guard trips cleanly on cycle 3 and 100 subsequent calls are all suppressed':
       - Without advancing time between calls, call hardReset() three times in a row.
       - The 3rd call must set loopActive=true and emit show-magicline-error {variant:'reset-loop'}.
       - Then call hardReset() 100 more times — every one must be suppressed via the loop-active path.
       - Assert the call-log contains exactly 2 destroy+create pairs (from cycles 1 and 2), not 102.

    5. Test case 'harness: clearStorageData storages array is exactly the 6 required types, every cycle':
       - Drive 10 cycles, for each cycle inspect the call-log entry for clearStorageData and assert options.storages deep-equals ['cookies','localstorage','sessionstorage','serviceworkers','indexdb','cachestorage'].

    The harness file exits with exit code 0 on success; any failure in any cycle fails the test. Document in a header comment: "Phase 4 IDLE-04 acceptance harness. IDLE-04 requirement text literally says '100 repeated reset cycles in a row never produce a half-logged-in state.' This file is the automated proof of that requirement."

    Use `node:test` features — specifically t.mock.timers with `apis: ['setTimeout','setInterval','Date']`. Between test cases call sessionReset._resetForTests() to clear module-scoped state.
  </action>
  <verify>
    <automated>cd C:/Users/Nico/vscode/bsfpos; node --test test/sessionReset.harness.js</automated>
  </verify>
  <acceptance_criteria>
    - File test/sessionReset.harness.js exists
    - grep "100" test/sessionReset.harness.js matches at least 4 times (loop count in 3 cases + documentation)
    - grep "sessionReset._resetForTests" test/sessionReset.harness.js matches at least once (state reset between cases)
    - grep "IDLE-04" test/sessionReset.harness.js matches in a header comment or test title
    - grep "'splash:show'" test/sessionReset.harness.js matches at least once (step 5 verification)
    - grep "reset-loop" test/sessionReset.harness.js matches at least once
    - grep "clearStorageData" test/sessionReset.harness.js matches at least twice
    - grep "flushStore" test/sessionReset.harness.js matches at least once
    - node --test test/sessionReset.harness.js exits 0 with all 4 test cases passing
    - No test case is skipped via t.skip() or only true
  </acceptance_criteria>
  <done>
    The 100-cycle harness runs green. IDLE-04's REQUIREMENT-SPEC "100 repeated reset cycles" is the literal test count. All 4 cases cover normal cycles, concurrent-suppression cycles, loop-guard trip, and storages whitelist exactness.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Phase 4 integration test suite (cross-module wiring)</name>
  <files>test/phase4-integration.test.js</files>
  <read_first>
    - src/main/authFlow.js (reducer entry points for cash-register-ready)
    - src/main/idleTimer.js (start/stop/expired entry points)
    - src/main/sessionReset.js (hardReset entry point)
    - src/main/magiclineView.js (KNOWN_EVENT_TYPES + handleInjectEvent structure)
    - test/authFlow.test.js (existing integration patterns)
    - test/phase3-integration.test.js (Phase 3 integration test conventions — use the same style)
    - .planning/phases/04-nfc-input-idle-session-lifecycle/04-03-PLAN.md (the wiring this test covers)
  </read_first>
  <action>
    Create test/phase4-integration.test.js — integration tests covering the cross-module wiring that Plan 03 introduced. These are NOT unit tests (those live in the individual test files); these prove the wiring contract end-to-end with mocks only where electron APIs are unavailable.

    Test cases:

    1. 'integration: authFlow BOOTING → cash-register-ready reduction includes start-idle-timer side-effect'
       - Requires only authFlow.reduce — pure function, no electron needed
       - Assert the sideEffects array contains exactly one entry with kind 'start-idle-timer'

    2. 'integration: authFlow LOGIN_SUBMITTED → cash-register-ready reduction includes start-idle-timer side-effect'
       - Same for the second reducer path

    3. 'integration: magiclineView KNOWN_EVENT_TYPES contains product-search-focused, product-search-blurred, activity'
       - Require magiclineView with a minimal mock of electron to allow it to load (the module has top-level requires) — use require.cache injection as in test/sessionReset.test.js
       - Access the exported KNOWN_EVENT_TYPES (if not directly exported, add an underscore-prefixed test-only export in magiclineView.js as part of Plan 03 — but since Plan 03 is locked, instead verify via a grep-based test that reads the source file and asserts the strings appear in the Set definition). Prefer a source-file grep test to avoid touching Plan 03 module exports.

    4. 'integration: idleTimer.expired() calls sessionReset.hardReset({reason:"idle-expired"})'
       - Mock sessionReset via require.cache to capture the hardReset call
       - Call idleTimer.init(mockMw), idleTimer.start(), idleTimer.expired()
       - Assert captured args deep-equal { reason: 'idle-expired' }

    5. 'integration: sessionReset.hardReset with reason "crash" is counted toward the unified rolling window'
       - Call hardReset with reason 'idle-expired', then 'crash', then 'idle-expired' within the same window (no time advance)
       - Assert the 3rd call trips loopActive=true AND the resetTimestamps array contains all three reasons (proves D-18 unified counter)

    6. 'integration: render-process-gone listener skips recovery when details.reason === "clean-exit"' (research pin #6)
       - This is a source-file grep test: read src/main/magiclineView.js, assert it contains the exact string `details.reason === 'clean-exit'` followed by a return statement

    7. 'integration: preload.js exposes the 5 Phase 4 IPC entries on the kiosk surface' (source-file grep)
       - Read src/main/preload.js and assert it contains onShowIdleOverlay, onHideIdleOverlay, notifyIdleDismissed, notifyIdleExpired, requestResetLoopRecovery
       - Assert the IPC channel names appear: 'show-idle-overlay', 'hide-idle-overlay', 'idle-dismissed', 'idle-expired', 'request-reset-loop-recovery'

    8. 'integration: inject.js Phase 4 listeners are AFTER the idempotency guard anchor' (source-file line number test)
       - Read src/inject/inject.js, find the line number of `window.__bskiosk_injected__ = true`
       - Find the line numbers of 'focusin', 'focusout', 'pointerdown', 'touchstart', and the closest to '[data-role="button"]' (the post-sale click listener)
       - Assert all Phase 4 listener line numbers are GREATER than the anchor line number (research pin #4, Pitfall 4)

    9. 'integration: JETZT_VERKAUFEN_TEXT appears exactly once as the authoritative literal' (source-file grep)
       - Count occurrences of the literal 'Jetzt verkaufen' across src/inject/*.js
       - Assert exactly 1 occurrence (the fragile-selectors.js declaration OR the inject.js top-of-file fallback — whichever strategy Plan 04 chose)

    Use node:test + node:assert. Keep the file under 250 lines if possible — prefer strong assertions over redundant cases.
  </action>
  <verify>
    <automated>cd C:/Users/Nico/vscode/bsfpos; node --test test/phase4-integration.test.js; node --test test/authFlow.test.js test/badgeInput.test.js test/idleTimer.test.js test/sessionReset.test.js test/sessionReset.harness.js test/phase4-integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File test/phase4-integration.test.js exists
    - grep -c "^\s*test(" test/phase4-integration.test.js returns at least 9 (the 9 integration cases above)
    - grep "start-idle-timer" test/phase4-integration.test.js matches at least twice (branches 1 and 2)
    - grep "unified rolling window\|D-18" test/phase4-integration.test.js matches at least once
    - grep "clean-exit" test/phase4-integration.test.js matches at least once
    - grep "onShowIdleOverlay\|onHideIdleOverlay\|requestResetLoopRecovery" test/phase4-integration.test.js matches at least 3 times
    - grep "window.__bskiosk_injected__" test/phase4-integration.test.js matches at least once (line-number check)
    - grep "Jetzt verkaufen" test/phase4-integration.test.js matches at least once
    - node --test test/phase4-integration.test.js exits 0 with all 9 tests passing
    - The full Phase 1-4 unit + harness + integration suite passes: node --test test/authFlow.test.js test/badgeInput.test.js test/idleTimer.test.js test/sessionReset.test.js test/sessionReset.harness.js test/phase4-integration.test.js — all green
    - Phase 3 existing integration test (test/phase3-integration.test.js) still passes (no regressions)
  </acceptance_criteria>
  <done>
    Integration test suite covers all Plan 03 wiring: authFlow side-effect, KNOWN_EVENT_TYPES whitelist, idleTimer→sessionReset delegation, unified rolling counter, clean-exit guard, preload surface, inject.js listener placement, JETZT_VERKAUFEN_TEXT authoritative literal.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Produce 04-VERIFICATION.md pre-filled checklist for human testing</name>
  <files>.planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (NFC-01..06, IDLE-01..07)
    - .planning/ROADMAP.md (Phase 4 6 success criteria — used as the verification spine)
    - .planning/phases/01-locked-down-shell-os-hardening/01-VERIFICATION.md (Phase 1 verification doc format — mirror the structure)
    - .planning/phases/04-nfc-input-idle-session-lifecycle/04-UI-SPEC.md (visual expectations to check against)
  </read_first>
  <action>
    Create .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md following the Phase 1 verification doc format. Sections:

    1. Header: phase, plan, status fields in frontmatter (not yaml — just plain header)

    2. Automated Verification Summary — a table listing each test file and its pass/fail status. To be filled in by the executor who runs all suites one last time and records counts:
       - test/idleTimer.test.js
       - test/badgeInput.test.js
       - test/sessionReset.test.js
       - test/authFlow.test.js (existing + 3 new Phase 4 reducer tests)
       - test/sessionReset.harness.js (4 cases, 100 cycles within case 1)
       - test/phase4-integration.test.js (9 cases)
       - node --check on all modified src files (one line each)

    3. Human Verification Checklist — one subsection per requirement, with 13 groups:
       - NFC-01: Plug in Deka reader to production kiosk. Cold boot. Scan a test badge on the FIRST-EVER key capture opportunity after boot. EXPECTED: member name appears in customer field within 1s. FAIL CASE: first character is dropped (the legacy bug). Record: PASS / FAIL / N/A. Tester initial. Date.
       - NFC-02: Scan 5 badges in quick succession. All 5 must commit and update the customer field. Record.
       - NFC-03: Dedicated regression — first scan after a hard reset (via idle expiry) must also capture the leading character. Reproduce steps: let kiosk sit idle 90s to trigger reset, then immediately scan. Record.
       - NFC-04: Scan a badge with a known member. Inspect customer field textContent in devtools remote or visually. Value must match badge. Record.
       - NFC-05: While idle overlay is visible, scan a badge. Expected: overlay dismisses, 60s timer restarts, badge is NOT forwarded to Magicline (absorbed as dismiss input). Record.
       - NFC-06: Focus the Magicline product-search field by tapping it. Scan a product barcode. Expected: the barcode types into the product-search field normally (no customer-search injection). Record.
       - IDLE-01: Touch nothing for 60s. Expected: branded idle overlay appears with visible 30s countdown. German copy "Noch da?", large yellow countdown, SEKUNDEN label, "Weiter" button. Record visual pass per UI-SPEC contract.
       - IDLE-02: During the idle countdown, tap anywhere on screen. Expected: overlay dismisses, Magicline state (cart, customer) unchanged. Record.
       - IDLE-03: Let the idle countdown expire (touch nothing for 90s total). Expected: splash briefly, then fresh cash register, no customer, no cart. Record.
       - IDLE-04: Repeat IDLE-03 twice manually, wait 90s between each to avoid the loop guard. Expected: both resets succeed cleanly. Confirm main.log shows two sessionReset.hardReset: reason=idle-expired lines. Record. (The 100-cycle automated harness in Task 1 covers the rest; this is human spot-check.)
       - IDLE-05: Trigger 3 rapid resets within 60s by repeatedly force-killing the Magicline renderer (Task Manager → find the Bee Strong POS renderer process → End Task) 3x within 60s. Expected: after the 3rd kill, the branded reset-loop error appears with "Kiosk muss neu gestartet werden" and PIN eingeben button. Enter admin PIN. Expected: app.relaunch(); app.quit() — kiosk restarts. Record.
       - IDLE-06: Add a product to cart, tap "Jetzt verkaufen". Count 3 seconds. Expected: customer-search field clears automatically, the sale remains in Magicline's history. Record.
       - IDLE-07: Force-kill the Magicline renderer once via Task Manager. Expected: log line magicline.render-process-gone with details, then splash briefly, then clean re-login. No reset-loop guard trip on first kill. Record.

    4. Deferred Visual Checks (to fold into the next physical kiosk visit per STATE.md verification debt):
       - Confirm the OPAQUE idle overlay prevents any prior member data bleed-through (UI-SPEC §Color reinterpretation)
       - Confirm the 80px countdown number is readable at arm's length (approx 50-80cm)
       - Confirm dismiss-anywhere behavior: a tap at any point on the overlay (not just on the button) dismisses

    5. Sign-off block: Tester name, Date, Overall verdict (PASS / PARTIAL / FAIL), Next action.

    Every checklist item must reference its requirement ID in square brackets, e.g. [NFC-01]. Every item must have a defined PASS condition (concrete, observable) and a defined FAIL condition.

    Keep the file human-readable markdown. No YAML frontmatter beyond the optional header block. The document is filled in during the human checkpoint (Task 4) — leave PASS/FAIL columns as [ ] empty checkboxes with space for tester initials.
  </action>
  <verify>
    <automated>cd C:/Users/Nico/vscode/bsfpos; test -f .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md && grep -c "NFC-0\|IDLE-0" .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the expected path
    - grep "NFC-01" .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md matches at least once
    - grep -c "NFC-0[1-6]" .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md returns at least 6
    - grep -c "IDLE-0[1-7]" .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md returns at least 7
    - grep "Noch da" .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md matches at least once (visual check reference)
    - grep "Kiosk muss neu gestartet werden" .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md matches at least once
    - grep "100-cycle\|harness" .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md matches at least once
    - grep "PASS\|FAIL" .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md matches at least 13 times (one per requirement)
    - grep "Sign-off\|Tester" .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md matches at least once
    - File is well-formed markdown (visual inspection during checker pass)
  </acceptance_criteria>
  <done>
    04-VERIFICATION.md exists with one checklist item per requirement, pre-filled PASS/FAIL conditions, and an automated-suite summary section at the top.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human checkpoint — physical kiosk verification against 04-VERIFICATION.md</name>
  <files>.planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md</files>
  <action>This is a BLOCKING human checkpoint. Claude MUST NOT proceed past this task without user sign-off. The executor presents the instructions in <how-to-verify> below to the user, waits for the resume-signal, and on approval updates 04-VERIFICATION.md with the tester initials and verdict. No automated action by Claude.</action>
  <verify>Human sign-off captured in .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md with explicit PASS/FAIL per requirement ID.</verify>
  <done>User has typed the resume-signal and 04-VERIFICATION.md has been committed to git with the final verdict.</done>
  <what-built>
    All 13 Phase 4 requirements wired end-to-end:
    - badgeInput.js + idleTimer.js + sessionReset.js main modules (Plans 01-02)
    - magiclineView/authFlow/main.js/preload.js wiring (Plan 03)
    - inject.js/fragile-selectors/host.html/host.css/host.js renderer wiring (Plan 04)
    - Unit tests (4 suites), integration tests (9 cases), 100-cycle harness (4 cases)
    - 04-VERIFICATION.md human checklist ready for fill-in
  </what-built>
  <how-to-verify>
    Follow the checklist in .planning/phases/04-nfc-input-idle-session-lifecycle/04-VERIFICATION.md against the physical kiosk (or the proxy dev box if kiosk hardware is unavailable — note the substitution in the sign-off block).

    Required hardware:
    1. The Bee Strong POS Windows terminal (or the proxy box DESKTOP-P1E98A1 used in 03-09)
    2. The Deka USB HID badge reader, plugged in
    3. A test badge with a known member in the Magicline staging account
    4. Task Manager access for the crash-simulation steps

    Test sequence (follow 04-VERIFICATION.md exactly, in order):
    1. Fresh cold boot → first scan (NFC-01, NFC-03 first-char-drop regression)
    2. Rapid 5-badge burst (NFC-02)
    3. Scan + product barcode pass-through (NFC-04, NFC-06)
    4. Idle 60s → overlay visual + German copy + countdown (IDLE-01)
    5. Mid-countdown tap dismiss + cart preserved (IDLE-02)
    6. Let countdown expire → splash → clean cash register (IDLE-03)
    7. Second manual idle reset spaced 90s later (IDLE-04 spot check)
    8. 3 rapid Task Manager kills of Magicline renderer within 60s → reset-loop error → admin PIN → app.relaunch (IDLE-05)
    9. Sale → Jetzt verkaufen → 3s customer-field clear (IDLE-06)
    10. Single Task Manager kill → splash → clean re-login (IDLE-07)
    11. While overlay visible, scan badge → dismisses overlay, badge absorbed (NFC-05)

    For each step: fill in the corresponding checklist row with PASS or FAIL and tester initials + date. Capture main.log excerpts for any FAIL cases.

    Expected structured log lines to check in %AppData%/Bee Strong POS/logs/main.log during testing:
    - badgeInput.commit: length=N (where N is badge length, NEVER content)
    - idleTimer.state: IDLE -> OVERLAY_SHOWING reason=timeout
    - sessionReset.hardReset: reason=idle-expired count=1
    - sessionReset.hardReset: reason=crash count=1
    - sessionReset.loop-detected: count=3 reasons=[idle-expired,crash,crash] (or similar)
    - magicline.render-process-gone: {reason:"killed", ...}

    After filling in the checklist, commit the updated 04-VERIFICATION.md to git.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 13 requirements pass the checklist.
    Type "approved-with-deferred {list}" if specific items are deferred to the next kiosk visit (e.g. visual checks per STATE.md verification debt).
    Type "failed {requirement-ids}" if any requirement fails — the planner will re-enter revision mode for gap closure.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated harness → sessionReset module | Harness mocks electron APIs — no real session data touched |
| Human tester → physical kiosk | Human holds real badges — badge content should NEVER appear in any log or harness output |
| Test fixtures → git | Test files must not commit real badge numbers or real Magicline credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-25 | Information Disclosure | Harness or integration test logging committed badge content | mitigate | Harness uses mock wc.executeJavaScript — captures only the injection JS string, which contains JSON.stringify(committed). Test assertions check the escape behavior with a controlled test string like 'TESTBADGE-12345', NOT real member data. Acceptance criteria grep checks ensure no real badges appear. |
| T-04-26 | Information Disclosure | 04-VERIFICATION.md capturing real badge IDs in "steps reproduced" fields | mitigate | Checklist instructions explicitly tell the tester to use a TEST badge tied to a staging-account member, and to redact any real member names when filling in failure notes. Documented in the sign-off block. |
| T-04-27 | Tampering | Human skipping the 100-cycle harness and passing IDLE-04 based on 2 manual resets | mitigate | Task 1 IS the 100-cycle acceptance artifact — it is automated and runs via node --test. Task 3 human checklist explicitly references the harness result in its IDLE-04 row ("spot check — the 100-cycle harness in Task 1 covers the rest"). The PASS condition for IDLE-04 requires BOTH the harness green AND the 2 manual resets. |
| T-04-28 | Denial of Service | Simulated reset-loop leaves kiosk in loopActive=true state between human verification steps | accept | The checklist is explicit that IDLE-05 testing is the LAST item before sign-off (app.relaunch) because it triggers the admin recovery. Alternative is to call sessionReset._resetForTests() via a devtools hook, which is not exposed in production. Document in checklist that IDLE-05 must be run last. |
| T-04-29 | Repudiation | Tester skipping checklist items without marking them | mitigate | Every row has explicit PASS/FAIL/N/A columns and tester initial field. Sign-off block requires overall verdict matching row counts. Orchestrator auditor (if run) can grep for empty checkboxes. |
</threat_model>

<verification>
- All automated suites green: node --test test/idleTimer.test.js test/badgeInput.test.js test/sessionReset.test.js test/sessionReset.harness.js test/phase4-integration.test.js test/authFlow.test.js
- All Phase 1/2/3 existing tests still pass (regression check)
- test/sessionReset.harness.js executes the literal 100-cycle loop successfully (IDLE-04 acceptance)
- 04-VERIFICATION.md exists with one checklist item per requirement (13 minimum)
- Human checkpoint completed with signed-off pass/fail for all 13 requirements, any failures captured as gap items for potential /gsd-plan-phase --gaps follow-up
</verification>

<success_criteria>
- 100-cycle harness runs green without mutex leak, loop-guard false trip, or step-order violation
- Integration test suite exercises the cross-module wiring with 9+ passing cases
- Human verification document is filled in with PASS/FAIL for every requirement ID (NFC-01..06, IDLE-01..07)
- Overall human verdict is PASS or PASS-with-deferred; any hard FAIL triggers gap-closure re-planning
- No requirement is left UNVERIFIED — every ID has an automated artifact or a human checklist row (or both)
</success_criteria>

<output>
After completion, create `.planning/phases/04-nfc-input-idle-session-lifecycle/04-05-SUMMARY.md` and `.planning/phases/04-nfc-input-idle-session-lifecycle/04-PHASE-SUMMARY.md`. The per-plan summary documents:
- Final harness cycle count (should be 100 per case 1)
- Integration test count
- Human checklist final verdict + any deferred items
- Log line samples captured from main.log during testing (redacted of any real badge content)
- Phase 4 total test count (unit + integration + harness) added during this phase
- Any follow-up TODOs discovered (e.g. BADGE_SPEED_MS tuning if a tester reports dropped chars)

The phase summary rolls up all 5 plans, all requirements, remaining verification debt, and readiness for Phase 5.
</output>
