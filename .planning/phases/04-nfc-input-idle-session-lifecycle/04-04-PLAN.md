---
phase: 04-nfc-input-idle-session-lifecycle
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/inject/inject.js
  - src/inject/fragile-selectors.js
  - src/host/host.html
  - src/host/host.css
  - src/host/host.js
autonomous: true
requirements: [NFC-06, IDLE-01, IDLE-02, IDLE-06]
tags: [electron, renderer, inject, overlay, ui]

must_haves:
  truths:
    - "inject.js emits product-search-focused and product-search-blurred drain events on focusin/focusout inside [data-role=product-search]"
    - "inject.js emits rAF-debounced activity drain events on document pointerdown + touchstart"
    - "inject.js adds a click listener on [data-role=button] with textContent===JETZT_VERKAUFEN_TEXT that clears customer-search input 3000ms later"
    - "All 3 new inject.js listeners are attached AFTER the idempotency guard (after window.__bskiosk_injected__ = true), NOT inside the early-return block (research pin #4)"
    - "JETZT_VERKAUFEN_TEXT is a named constant in fragile-selectors.js (drift-patch blast radius per D-21)"
    - "host.html gains a #idle-overlay div at z-index layer 200 slot per 04-UI-SPEC exact DOM"
    - "host.css gains .bsk-layer--idle + 6 idle classes per 04-UI-SPEC verbatim"
    - "host.js show-idle-overlay handler starts a 30s setInterval updating #idle-countdown-number, fires notifyIdleExpired at 0"
    - "host.js dismiss() is bound to button click AND #idle-overlay pointerdown/touchstart/keydown — single function both paths"
    - "host.js variant-switch gains reset-loop case that wires #error-pin-button to requestResetLoopRecovery (not requestPinRecovery)"
  artifacts:
    - path: "src/inject/inject.js"
      provides: "focusin/focusout listeners + rAF activity emitter + post-sale click listener"
    - path: "src/inject/fragile-selectors.js"
      provides: "JETZT_VERKAUFEN_TEXT structural-text constant"
    - path: "src/host/host.html"
      provides: "#idle-overlay DOM at z-index layer 200"
    - path: "src/host/host.css"
      provides: "7 new CSS classes for the idle overlay"
    - path: "src/host/host.js"
      provides: "show/hide idle overlay handlers + dismiss() + reset-loop variant case"
  key_links:
    - from: "inject.js focusin listener"
      to: "emit('product-search-focused')"
      via: "drain queue pattern"
      pattern: "product-search-focused"
    - from: "inject.js click listener"
      to: "__bskiosk_setMuiValue(input, empty-string)"
      via: "3s setTimeout"
      pattern: "setMuiValue"
    - from: "host.js show-idle-overlay"
      to: "notifyIdleExpired / notifyIdleDismissed"
      via: "setInterval countdown + dismiss()"
      pattern: "notifyIdle"
    - from: "host.js reset-loop variant"
      to: "requestResetLoopRecovery"
      via: "#error-pin-button click handler"
      pattern: "requestResetLoopRecovery"
---

<objective>
Wire the renderer half of Phase 4: inject.js gains three page-world listeners (product-search focus arbitration, touch activity emitter, post-sale clear), fragile-selectors.js gains the Jetzt-verkaufen drift constant, host.html/css/js gain the #idle-overlay DOM + styles + handlers + reset-loop variant case per 04-UI-SPEC.md.

This plan is independent of Plan 03 at the file level (zero file overlap) and runs parallel in Wave 2. It depends only on Plans 01 and 02. The renderer-side IPC surface it assumes (onShowIdleOverlay, notifyIdleExpired, etc.) is contractually created by Plan 03; at runtime both plans must complete before integration testing in Plan 05.

Purpose: Deliver IDLE-01 (branded overlay + 30s countdown), IDLE-02 (dismiss restores cart), IDLE-06 (post-sale clear), and NFC-06 (product-search arbitration signals).
Output: One extended inject file, one fragile-selectors file, three extended host UI files — all per UI-SPEC.md normative contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-nfc-input-idle-session-lifecycle/04-CONTEXT.md
@.planning/phases/04-nfc-input-idle-session-lifecycle/04-RESEARCH.md
@.planning/phases/04-nfc-input-idle-session-lifecycle/04-UI-SPEC.md
@src/inject/inject.js
@src/inject/fragile-selectors.js
@src/host/host.html
@src/host/host.css
@src/host/host.js

<interfaces>
Contracts this plan depends on and must honor:

inject.js Phase 2 idempotency structure (normative anchor):
- Line approx 36: if (window.__bskiosk_injected__) { ...early return path, only re-runs hideDynamic/detectReady/detectLogin... }
- Line approx 42: window.__bskiosk_injected__ = true;  ← ALL new Phase 4 listeners go BELOW this line
- Line approx 48: function emit(type, payload) { ... } — Phase 4 reuses this helper for 3 new event types
- Line approx 62: window.__bskiosk_setMuiValue(input, value) — reused for post-sale clear

04-UI-SPEC §Component Inventory — #idle-overlay DOM (NORMATIVE, copy byte-for-byte into host.html at the Layer 200 slot):
- Outer div: id="idle-overlay", class="bsk-layer bsk-layer--idle", style="display:none;", aria-hidden="true", role="dialog", aria-label="Möchten Sie fortfahren?"
- Child 1: img src="assets/logo-dark.png" class="bsk-logo" alt="Bee Strong Fitness" width="160"
- Child 2: h1 class="bsk-heading bsk-idle-title" text "Noch da?"
- Child 3: div class="bsk-idle-countdown" aria-live="polite" containing span#idle-countdown-number.bsk-idle-number text "30" AND span.bsk-idle-seconds-label text "SEKUNDEN"
- Child 4: p class="bsk-idle-subtext" text "Tippe irgendwo, um fortzufahren."
- Child 5: button type="button" id="idle-dismiss-btn" class="bsk-btn bsk-btn--primary bsk-btn--idle-dismiss" text "Weiter"

04-UI-SPEC §CSS class additions (NORMATIVE — append to host.css verbatim):
- .bsk-layer--idle { z-index: 200; background: #1A1A1A; pointer-events: auto; }  (plus flex centering if .bsk-layer base does not provide it)
- .bsk-idle-title { font-size: 24px; font-weight: 700; color: #FFFFFF; margin: 16px 0 16px 0; text-align: center; line-height: 1.2; }
- .bsk-idle-countdown { display: flex; flex-direction: column; align-items: center; gap: 8px; }
- .bsk-idle-number { font-size: 80px; font-weight: 700; color: #F5C518; line-height: 1.0; font-variant-numeric: tabular-nums; }
- .bsk-idle-seconds-label { font-size: 14px; font-weight: 700; color: #9CA3AF; letter-spacing: 0.05em; text-transform: uppercase; }
- .bsk-idle-subtext { font-size: 16px; font-weight: 400; color: #FFFFFF; text-align: center; padding: 0 32px; margin: 16px 0 0 0; line-height: 1.5; }
- .bsk-btn--idle-dismiss { min-width: 80px; min-height: 80px; margin-top: 24px; font-size: 20px; font-weight: 700; padding: 16px 32px; }

04-UI-SPEC §host.js Countdown Behavior Contract (NORMATIVE):
On show-idle-overlay: show overlay (display:flex, aria-hidden=false), set textContent to "30", start setInterval 1000ms that decrements; on reaching 0 clearInterval, hide overlay, call notifyIdleExpired().
On dismiss() (button click OR overlay pointerdown/touchstart/keydown): clearInterval (guarded), hide overlay, call notifyIdleDismissed().
On hide-idle-overlay IPC: clearInterval (guarded), hide overlay (no IPC reply).

04-UI-SPEC §reset-loop error variant (NORMATIVE):
- title: "Kiosk muss neu gestartet werden"
- subtext: "Bitte Studio-Personal verständigen"
- PIN button visible with label "PIN eingeben"
- PIN button click calls window.kiosk.requestResetLoopRecovery() (NOT requestPinRecovery — variant-specific routing)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend inject.js with Phase 4 listeners and add JETZT_VERKAUFEN_TEXT to fragile-selectors.js</name>
  <files>src/inject/inject.js, src/inject/fragile-selectors.js</files>
  <read_first>
    - src/inject/inject.js (ENTIRE FILE — especially the idempotency guard around lines 36-42 and the emit() helper; determine the exact line number of window.__bskiosk_injected__ = true which is the anchor for Phase 4 additions)
    - src/inject/fragile-selectors.js (ENTIRE FILE — determine how existing STABLE + FRAGILE entries are exported and audited; mirror the pattern for the new structural-text entry)
    - src/main/magiclineView.js (find how inject.js and fragile-selectors.js are loaded into INJECT_JS — fs.readFileSync concatenation vs require — this determines where to declare JETZT_VERKAUFEN_TEXT so it is in scope inside inject.js)
    - .planning/phases/04-nfc-input-idle-session-lifecycle/04-CONTEXT.md (D-05, D-21)
    - .planning/phases/04-nfc-input-idle-session-lifecycle/04-RESEARCH.md (Pattern 3 product-search arbitration, Pattern 7 post-sale clear, Pattern 10 idempotency contract, Pitfall 4)
    - docs/BeeStrong_POS_Kiosk_Project.md (prototype lines approx 363-446 — source for verbatim porting of click listener)
  </read_first>
  <action>
    Edit A — fragile-selectors.js: add JETZT_VERKAUFEN_TEXT constant.

    Add the constant following the existing export pattern in fragile-selectors.js. The literal value must be exactly 'Jetzt verkaufen'. If fragile-selectors.js uses categorized exports (STABLE / FRAGILE / STRUCTURAL_TEXT), place the new entry in the STRUCTURAL_TEXT category (create it if missing, following the naming convention of the existing categories so the Phase 2 boot-time selector self-check audit log includes it as a structural-text entry). If the existing file is a flat module.exports object, append a top-level JETZT_VERKAUFEN_TEXT key whose value is the literal.

    Edit B — inject.js: three new listeners INSIDE the one-time setup path.

    CRITICAL (research pin #4 + Pattern 10): all three new listeners must be added AFTER the line `window.__bskiosk_injected__ = true;` (around line 42) and NOT inside the `if (window.__bskiosk_injected__)` early-return block. Listeners inside the early-return block would re-attach on every re-injection and fire N× per event.

    First determine how JETZT_VERKAUFEN_TEXT is made visible to inject.js:
    - If magiclineView.js concatenates fragile-selectors.js content into INJECT_JS before inject.js content (fs.readFileSync for both, joined as strings), the constant is already in scope as a global inside the injected page world. Use `JETZT_VERKAUFEN_TEXT` directly.
    - If magiclineView.js uses a different loading pattern (e.g. only inject.js is read), declare `var JETZT_VERKAUFEN_TEXT = 'Jetzt verkaufen';` at the top of inject.js (inside the IIFE, before the idempotency guard) AND keep the authoritative entry in fragile-selectors.js for the audit log. Both occurrences must be byte-equal. Document which path was chosen in the SUMMARY.

    Now add the following block IMMEDIATELY AFTER the `window.__bskiosk_injected__ = true;` line:

    1) Product-search focus arbitration (D-05, NFC-06). Uses focusin/focusout (bubbles) — NOT focus/blur (don't bubble):
       - document.addEventListener('focusin', handler) where handler queries `[data-role="product-search"]` and emits 'product-search-focused' when e.target is inside it
       - document.addEventListener('focusout', handler) mirror, emits 'product-search-blurred'
       - Use the existing emit(type, payload) helper with empty object payload.

    2) rAF-debounced activity emitter (D-09 #3, IDLE-01 bump source). MUI churn produces dozens of synthetic events per second; rAF coalesces them to one emit per frame:
       - Module-scoped boolean _activityPending = false
       - _scheduleActivityEmit() returns early if _activityPending is true; sets it true; calls requestAnimationFrame(() => { _activityPending = false; emit('activity', {}); })
       - document.addEventListener('pointerdown', _scheduleActivityEmit, true)
       - document.addEventListener('touchstart',  _scheduleActivityEmit, true)
       - Third argument `true` (capture phase) ensures the emit fires even if Magicline stopPropagation()'s the event at a deeper handler.

    3) Post-sale clear (D-21, IDLE-06) — VERBATIM port from prototype lines approx 441-446:
       - document.addEventListener('click', handler)
       - Handler: var btn = e.target && e.target.closest && e.target.closest('[data-role="button"]')
       - If btn exists and btn.textContent.trim() === JETZT_VERKAUFEN_TEXT: setTimeout 3000ms callback that queries '[data-role="customer-search"] input' and calls window.__bskiosk_setMuiValue(input, '') if both the input and the setter exist.
       - Exactly 3000ms — the 3s delay is specified in IDLE-06 requirement text.

    Constraints:
    - DO NOT add listeners inside the idempotency-guard early-return block (Pitfall 4).
    - DO NOT use focus/blur (they don't bubble).
    - DO NOT skip the rAF coalescing (D-09 #3).
    - DO NOT inline the 'Jetzt verkaufen' string literal anywhere other than the authoritative declaration (fragile-selectors.js or the inject.js top-of-file constant if the concatenation path does not apply).
    - DO NOT log badge content or sale content anywhere in inject.js.
  </action>
  <verify>
    <automated>cd C:/Users/Nico/vscode/bsfpos; node --check src/inject/inject.js; node --check src/inject/fragile-selectors.js; node --test test/idleTimer.test.js test/badgeInput.test.js test/sessionReset.test.js test/authFlow.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep JETZT_VERKAUFEN_TEXT src/inject/fragile-selectors.js matches at least once
    - grep "'Jetzt verkaufen'" src/inject/fragile-selectors.js matches exactly once (the authoritative literal)
    - grep "product-search-focused" src/inject/inject.js matches exactly once (the emit call)
    - grep "product-search-blurred" src/inject/inject.js matches exactly once
    - grep "emit('activity'" src/inject/inject.js matches exactly once
    - grep "'focusin'" src/inject/inject.js matches at least once
    - grep "'focusout'" src/inject/inject.js matches at least once
    - grep "'pointerdown'" src/inject/inject.js matches at least once
    - grep "'touchstart'" src/inject/inject.js matches at least once
    - grep requestAnimationFrame src/inject/inject.js matches at least once (rAF debounce)
    - grep JETZT_VERKAUFEN_TEXT src/inject/inject.js matches at least once (reference or declaration)
    - grep 3000 src/inject/inject.js matches at least once (3s timeout)
    - grep __bskiosk_setMuiValue src/inject/inject.js matches at least twice (existing helper plus new post-sale clear call)
    - Line number ordering: grep -n for the anchor `window.__bskiosk_injected__ = true` vs line numbers of focusin/focusout/pointerdown/touchstart/click — all new listener lines must be GREATER than the anchor line number
    - node --check src/inject/inject.js exits 0
    - node --check src/inject/fragile-selectors.js exits 0
    - All 4 unit test suites still pass (test/idleTimer.test.js test/badgeInput.test.js test/sessionReset.test.js test/authFlow.test.js)
  </acceptance_criteria>
  <done>
    inject.js has three new listeners in the one-time setup path (focusin/focusout, rAF activity, post-sale click). fragile-selectors.js exports JETZT_VERKAUFEN_TEXT. The 'Jetzt verkaufen' literal exists in exactly one authoritative location. No regressions in unit tests.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend host.html + host.css with #idle-overlay DOM and 7 new CSS classes per UI-SPEC</name>
  <files>src/host/host.html, src/host/host.css</files>
  <read_first>
    - src/host/host.html (ENTIRE FILE — find the Layer 200 slot comment reserved in Phase 1, the existing #splash and #magicline-error elements, and the existing .bsk-layer base class reference)
    - src/host/host.css (ENTIRE FILE — find brand tokens, .bsk-layer base class, .bsk-btn--primary, .bsk-heading definitions; Phase 4 REUSES these classes without modification)
    - .planning/phases/04-nfc-input-idle-session-lifecycle/04-UI-SPEC.md (§Component Inventory and §CSS class additions — NORMATIVE copy-paste source)
  </read_first>
  <action>
    Edit A — host.html: add #idle-overlay at Layer 200 slot.

    Find the comment in host.html reserving Layer 200 for Phase 4 idle overlay (Phase 1 placed it between #splash and #magicline-error in the z-index ladder). Replace the placeholder comment with the exact DOM block from 04-UI-SPEC §Component Inventory (reproduced in the <interfaces> section above). Copy byte-for-byte:

    Outer element: div with id="idle-overlay", class="bsk-layer bsk-layer--idle", style="display:none;", aria-hidden="true", role="dialog", aria-label set to the exact German string "Möchten Sie fortfahren?".

    Children in order:
    1. img element — src="assets/logo-dark.png", class="bsk-logo", alt="Bee Strong Fitness", width="160"
    2. h1 element — class="bsk-heading bsk-idle-title", text content exactly "Noch da?"
    3. div element — class="bsk-idle-countdown", aria-live="polite", containing:
       - span element — id="idle-countdown-number", class="bsk-idle-number", text content exactly "30"
       - span element — class="bsk-idle-seconds-label", text content exactly "SEKUNDEN"
    4. p element — class="bsk-idle-subtext", text content exactly "Tippe irgendwo, um fortzufahren."
    5. button element — type="button", id="idle-dismiss-btn", class="bsk-btn bsk-btn--primary bsk-btn--idle-dismiss", text content exactly "Weiter"

    Preserve ALL German copy EXACTLY with correct umlauts: "Noch da?", "SEKUNDEN", "Tippe irgendwo, um fortzufahren.", "Weiter", "Möchten Sie fortfahren?". Use UTF-8 encoding for the file. The file must remain valid HTML after the edit.

    Edit B — host.css: append 7 new classes verbatim from UI-SPEC.

    Append the following class blocks to host.css (at the end of the file, or inside a Phase 4 section comment if one already exists). Values are normative — do not paraphrase or shorten:

    1. .bsk-layer--idle — properties: z-index 200, background #1A1A1A, pointer-events auto. ALSO add display flex, flex-direction column, align-items center, justify-content center UNLESS the base .bsk-layer class already provides them (read .bsk-layer first and decide; document the decision in the SUMMARY).
    2. .bsk-idle-title — font-size 24px, font-weight 700, color #FFFFFF, margin 16px 0 16px 0, text-align center, line-height 1.2.
    3. .bsk-idle-countdown — display flex, flex-direction column, align-items center, gap 8px.
    4. .bsk-idle-number — font-size 80px, font-weight 700, color #F5C518, line-height 1.0, font-variant-numeric tabular-nums.
    5. .bsk-idle-seconds-label — font-size 14px, font-weight 700, color #9CA3AF, letter-spacing 0.05em, text-transform uppercase.
    6. .bsk-idle-subtext — font-size 16px, font-weight 400, color #FFFFFF, text-align center, padding 0 32px, margin 16px 0 0 0, line-height 1.5.
    7. .bsk-btn--idle-dismiss — min-width 80px, min-height 80px, margin-top 24px, font-size 20px, font-weight 700, padding 16px 32px.

    Constraints:
    - DO NOT modify existing .bsk-layer, .bsk-btn, .bsk-btn--primary, or .bsk-heading definitions.
    - DO NOT change any brand tokens or add new color variables — all Phase 4 colors are drawn from Phase 1 palette (#1A1A1A, #FFFFFF, #F5C518, #9CA3AF).
    - DO NOT add transitions or animations — UI-SPEC forbids them on the countdown number.
    - Do NOT touch #magicline-error existing styles — the reset-loop variant reuses them verbatim.
  </action>
  <verify>
    <automated>cd C:/Users/Nico/vscode/bsfpos; grep -q 'id="idle-overlay"' src/host/host.html; grep -q 'id="idle-countdown-number"' src/host/host.html; grep -q 'id="idle-dismiss-btn"' src/host/host.html; grep -q 'Noch da' src/host/host.html; grep -q 'bsk-layer--idle' src/host/host.css; grep -q 'bsk-idle-number' src/host/host.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="idle-overlay"' src/host/host.html matches exactly once
    - grep 'id="idle-countdown-number"' src/host/host.html matches exactly once
    - grep 'id="idle-dismiss-btn"' src/host/host.html matches exactly once
    - grep 'role="dialog"' src/host/host.html matches at least once (may already exist for other dialogs)
    - grep 'aria-label="Möchten Sie fortfahren?"' src/host/host.html matches exactly once
    - grep 'Noch da?' src/host/host.html matches exactly once
    - grep 'SEKUNDEN' src/host/host.html matches exactly once
    - grep 'Tippe irgendwo, um fortzufahren.' src/host/host.html matches exactly once
    - grep '>Weiter<' src/host/host.html matches exactly once
    - grep 'aria-live="polite"' src/host/host.html matches at least once (on .bsk-idle-countdown)
    - grep 'assets/logo-dark.png' src/host/host.html matches at least once (may exist elsewhere from Phase 1)
    - grep 'bsk-layer--idle' src/host/host.css matches exactly once
    - grep 'bsk-idle-title' src/host/host.css matches exactly once
    - grep 'bsk-idle-countdown' src/host/host.css matches exactly once
    - grep 'bsk-idle-number' src/host/host.css matches exactly once
    - grep 'bsk-idle-seconds-label' src/host/host.css matches exactly once
    - grep 'bsk-idle-subtext' src/host/host.css matches exactly once
    - grep 'bsk-btn--idle-dismiss' src/host/host.css matches exactly once
    - grep '#F5C518' src/host/host.css matches at least twice (at minimum the existing phase 1 accent usages plus .bsk-idle-number)
    - grep 'z-index: 200' src/host/host.css matches exactly once
    - grep '80px' src/host/host.css matches at least twice (.bsk-idle-number font-size 80px and .bsk-btn--idle-dismiss min-width/min-height 80px)
    - grep 'tabular-nums' src/host/host.css matches exactly once
    - File must be UTF-8 — verify umlauts render correctly by grepping for 'fortfahren' (no mojibake)
  </acceptance_criteria>
  <done>
    host.html has #idle-overlay at z-index layer 200 with exact UI-SPEC DOM and German copy. host.css has 7 new classes with exact property values. No existing classes modified. No new brand tokens introduced.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend host.js with idle overlay countdown handlers, dismiss(), and reset-loop variant case</name>
  <files>src/host/host.js</files>
  <read_first>
    - src/host/host.js (ENTIRE FILE — find the existing variant-switch in showMagiclineError from Phase 3 D-09, the existing onShowSplash/onHideSplash handlers from Phase 1, and the existing #error-pin-button click handler from Phase 3 that wires requestPinRecovery)
    - src/host/host.html (confirm the #idle-overlay and #idle-dismiss-btn IDs match what Task 2 added)
    - src/main/preload.js (confirm the window.kiosk surface added in Plan 03 — onShowIdleOverlay, onHideIdleOverlay, notifyIdleDismissed, notifyIdleExpired, requestResetLoopRecovery)
    - .planning/phases/04-nfc-input-idle-session-lifecycle/04-UI-SPEC.md (§host.js Countdown Behavior Contract — NORMATIVE)
    - .planning/phases/04-nfc-input-idle-session-lifecycle/04-CONTEXT.md (D-11, D-19, D-20)
  </read_first>
  <action>
    Make three edits to host.js.

    Edit A — Add idle overlay show handler.

    Add a module-scoped variable near the top of host.js (or alongside other module-scope vars from Phase 1/2/3):
      var idleInterval = null;

    Register a handler via window.kiosk.onShowIdleOverlay(function () { ... }) whose body implements the D-11 / UI-SPEC countdown contract exactly:
    1. Look up #idle-overlay and #idle-countdown-number; if either is missing, return (defensive).
    2. If idleInterval is truthy, clearInterval(idleInterval) first (guard against double-show).
    3. Declare a local var countdown = 30.
    4. Set the number element's textContent to "30".
    5. Set overlay.style.display = "flex" and overlay.setAttribute("aria-hidden", "false").
    6. idleInterval = setInterval(function() { ... }, 1000) where the callback:
       - decrements countdown by 1
       - sets numEl.textContent = String(countdown)
       - if (countdown <= 0): clearInterval(idleInterval); idleInterval = null; hide overlay (display:none, aria-hidden=true); call window.kiosk.notifyIdleExpired();

    Edit B — Add dismiss() function and bind it to button + overlay listeners.

    Declare a top-level function dismissIdleOverlay() that:
    1. If idleInterval is truthy, clearInterval(idleInterval) and set idleInterval = null.
    2. Hide #idle-overlay (display:none, aria-hidden=true).
    3. Call window.kiosk.notifyIdleDismissed().

    Bind dismissIdleOverlay to:
    - document.getElementById('idle-dismiss-btn').addEventListener('click', dismissIdleOverlay)
    - document.getElementById('idle-overlay').addEventListener('pointerdown', dismissIdleOverlay)
    - document.getElementById('idle-overlay').addEventListener('touchstart', dismissIdleOverlay)
    - document.getElementById('idle-overlay').addEventListener('keydown', dismissIdleOverlay)

    Also register a hide handler via window.kiosk.onHideIdleOverlay(function () { ... }) that performs steps 1 and 2 of dismissIdleOverlay but does NOT call notifyIdleDismissed (main is the one pulling the overlay down in this path; no reply needed per UI-SPEC).

    Edit C — Add reset-loop case to the variant-switch in showMagiclineError (Phase 3 D-09 pattern).

    Find the existing variant-switch (look for `if (variant === 'credentials-unavailable')` or similar from Phase 3). Add a new branch:
      if (variant === 'reset-loop') {
        titleEl.textContent = 'Kiosk muss neu gestartet werden';
        subtextEl.textContent = 'Bitte Studio-Personal verständigen';
        pinButton.style.display = '';  // visible, reuses existing .bsk-btn--primary styling
        pinButton.textContent = 'PIN eingeben';
        // Variant-aware click target — reset-loop uses requestResetLoopRecovery, NOT requestPinRecovery
        pinButton.onclick = function () { window.kiosk.requestResetLoopRecovery(); };
        return;
      }

    Preserve existing Phase 3 variant branches (drift, credentials-unavailable, login-failed) UNCHANGED. The click handler reassignment via `pinButton.onclick = ...` for the reset-loop variant must NOT leak into the other variants — if host.js currently uses addEventListener, switch to direct .onclick assignment in ALL branches for consistency (so the handler is replaced, not stacked) or use removeEventListener + addEventListener in a consistent way. Do not create duplicate listeners across variant switches — that would cause the admin PIN button to fire multiple handlers in one click.

    Constraints:
    - DO NOT animate the countdown number — textContent swap per tick is the full behavior (UI-SPEC §Typography).
    - DO NOT place handlers at module top level before the DOM exists if host.js follows a DOMContentLoaded pattern — mirror the existing Phase 1/3 initialization order.
    - DO NOT touch any existing Phase 1/2/3 host.js code except the variant-switch.
    - NEVER log badge content or sale content in host.js.
  </action>
  <verify>
    <automated>cd C:/Users/Nico/vscode/bsfpos; node --check src/host/host.js; grep -q onShowIdleOverlay src/host/host.js; grep -q notifyIdleExpired src/host/host.js; grep -q notifyIdleDismissed src/host/host.js; grep -q requestResetLoopRecovery src/host/host.js; grep -q "reset-loop" src/host/host.js</automated>
  </verify>
  <acceptance_criteria>
    - grep onShowIdleOverlay src/host/host.js matches exactly once
    - grep onHideIdleOverlay src/host/host.js matches exactly once
    - grep notifyIdleDismissed src/host/host.js matches exactly once
    - grep notifyIdleExpired src/host/host.js matches exactly once
    - grep requestResetLoopRecovery src/host/host.js matches exactly once
    - grep "idleInterval" src/host/host.js matches at least 3 times (declaration, clearInterval guard, setInterval assignment)
    - grep "setInterval" src/host/host.js matches at least once
    - grep "clearInterval" src/host/host.js matches at least twice (show path guard, dismiss path)
    - grep "1000" src/host/host.js matches at least once (setInterval cadence)
    - grep "idle-dismiss-btn" src/host/host.js matches at least once
    - grep "idle-overlay" src/host/host.js matches at least 3 times (getElementById calls in show, dismiss, hide)
    - grep "idle-countdown-number" src/host/host.js matches at least once
    - grep "'pointerdown'" src/host/host.js matches at least once (dismiss binding)
    - grep "'touchstart'" src/host/host.js matches at least once
    - grep "'keydown'" src/host/host.js matches at least once
    - grep "'click'" src/host/host.js matches at least once (button click binding)
    - grep "'reset-loop'" src/host/host.js matches exactly once (variant-switch case)
    - grep "Kiosk muss neu gestartet werden" src/host/host.js matches exactly once
    - grep "Bitte Studio-Personal verständigen" src/host/host.js matches at least once (may already exist from credentials-unavailable variant)
    - grep "PIN eingeben" src/host/host.js matches at least once (may already exist from Phase 3)
    - grep "display: none\|display=.none.\|display =.none." src/host/host.js has at least 2 hits in the idle paths (show-interval-zero + dismiss + hide)
    - Existing variant branches from Phase 3 are still present: grep 'credentials-unavailable' matches at least once, grep 'login-failed' matches at least once
    - node --check src/host/host.js exits 0
    - All 4 unit test suites still pass (test/idleTimer.test.js test/badgeInput.test.js test/sessionReset.test.js test/authFlow.test.js)
  </acceptance_criteria>
  <done>
    host.js owns the 30s countdown setInterval, fires notifyIdleExpired at 0, has a single dismiss() function bound to button + 3 overlay event listeners, and has a new reset-loop variant case wiring the PIN button to requestResetLoopRecovery. Existing Phase 3 variant logic is preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Magicline page world (untrusted React DOM) → inject.js event listeners | inject.js runs in page context and listens to arbitrary page events; stopPropagation in third-party handlers could suppress activity/focus signals |
| host.html renderer → main process via preload window.kiosk | All renderer-to-main calls go through contextBridge surface; no raw ipcRenderer exposed |
| User touch/key input → dismiss handler | Any touch/key event on the idle overlay triggers dismiss — acceptable since dismiss only restores the 60s timer without privileged action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Tampering | Jetzt verkaufen button text drift or spoofed click | mitigate | Literal is a named constant in fragile-selectors.js (drift-patch blast radius). Click handler only clears the customer-search input — no privileged operation. Worst case of a drift incident is the customer field does not clear after a sale — visible to staff, low-risk. ASVS V5. |
| T-04-19 | Tampering | inject.js listener duplication causing N× emit per event | mitigate | Pattern 10 idempotency contract: all new listeners attached AFTER window.__bskiosk_injected__ = true, NOT inside the re-injection early-return block. Grep-verified in acceptance criteria. |
| T-04-20 | Denial of Service | pointerdown/touchstart flooding idleTimer.bump | mitigate | rAF debounce coalesces all touch/pointer events within one animation frame to a single emit. Upper bound 60 emits/sec which is cheap for clearTimeout+setTimeout. |
| T-04-21 | Information Disclosure | Idle overlay exposing prior member data via transparency | mitigate | UI-SPEC §Color reinterpretation: overlay background is OPAQUE #1A1A1A, NOT translucent. Prior member's cart/customer never visible through overlay. User decision 2026-04-10. ASVS V3. |
| T-04-22 | Tampering | Dismiss path accepting unexpected event types | accept | pointerdown/touchstart/keydown/click all route to the same dismiss() which only calls notifyIdleDismissed — no privilege escalation path. Any input is valid dismiss (UI-SPEC). |
| T-04-23 | Elevation of Privilege | reset-loop PIN button bypass via DOM manipulation | mitigate | The PIN button click calls requestResetLoopRecovery which main routes through the existing Phase 3 PIN modal flow. app.relaunch only fires after PIN verification (Plan 03 main handler). Direct DOM manipulation of the button cannot bypass the PIN gate. |
| T-04-24 | Repudiation | inject.js listeners without audit trail | accept | inject.js runs in page world and cannot write to electron-log directly. Audit trail for activity/focus events lives in main (magiclineView handleInjectEvent delegations from Plan 03). Plan 04 adds no new audit gaps. |
</threat_model>

<verification>
- All 4 existing unit test suites continue to pass
- Every grep-verifiable acceptance criterion above resolves
- node --check passes on inject.js, fragile-selectors.js, host.js
- host.html and host.css visually render the idle overlay correctly (deferred to Plan 05 human checkpoint)
- Listener line-number ordering proven: anchor window.__bskiosk_injected__ = true precedes all new listener attachments in inject.js
- Existing Phase 3 variant branches in host.js are unchanged (regression grep)
</verification>

<success_criteria>
- inject.js has three new one-time-setup listeners per research Pattern 3, 7 and D-09 #3
- fragile-selectors.js exports JETZT_VERKAUFEN_TEXT as the authoritative drift constant
- host.html has #idle-overlay at layer 200 with byte-for-byte UI-SPEC DOM
- host.css has 7 new classes matching UI-SPEC normative values
- host.js owns the 30s countdown setInterval, dismiss(), and reset-loop variant case
- Zero regressions in Plans 01/02 unit tests
- All Phase 1/2/3 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-nfc-input-idle-session-lifecycle/04-04-SUMMARY.md` documenting:
- Which strategy was used for JETZT_VERKAUFEN_TEXT (concatenation of fragile-selectors into INJECT_JS, or top-of-inject.js declaration fallback)
- Exact line number in inject.js where the idempotency anchor lives vs the new listeners
- Whether .bsk-layer base class already provided flex centering, or whether .bsk-layer--idle needed to add it
- Any Phase 3 variant handler mutations (e.g. if a common handler-reassignment pattern was introduced for the onclick target swap)
- Confirmation that the German copy renders without mojibake
</output>
