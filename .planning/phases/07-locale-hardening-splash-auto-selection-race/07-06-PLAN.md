---
phase: 07-locale-hardening-splash-auto-selection-race
plan: 06
type: execute
wave: 4
depends_on: [07-02, 07-04, 07-05]
files_modified:
  - docs/runbook/v1.1-KIOSK-VISIT.md
  - .planning/phases/07-locale-hardening-splash-auto-selection-race/07-VERIFICATION.md
autonomous: false
requirements:
  - LOCALE-01
  - SPLASH-01
must_haves:
  truths:
    - "Phase 07 unit-test gate (fragileSelectors.test.js + magiclineView.sentinel.test.js + logger.audit.test.js) passes mechanically before the human kiosk walk."
    - "On a kiosk with Windows set to English-US, Magicline serves the cash-register UI in German."
    - "5 consecutive welcome→tap→register→idle→welcome cycles each emit event=auto-select.result result=ok."
    - "Tapping anywhere on the splash during the auto-select window is swallowed — no tap reaches Magicline."
    - "splash:hide-final arrives before the 5500 ms host safety timeout on the happy path."
    - "Cold-boot and idle-recovery splash paths still fire splash:hide (unchanged behaviour)."
    - "Admin PIN modal (Ctrl+Shift+F12) is still reachable while the splash is in auto-select-pending state."
    - "A deliberately broken chain (DevTools DOM mutation) degrades with result=fail or result=timeout within ≤5500 ms."
  artifacts:
    - path: docs/runbook/v1.1-KIOSK-VISIT.md
      provides: "Phase 07 verification checklist with pass/fail fields"
      contains: "Phase 07 verification"
    - path: .planning/phases/07-locale-hardening-splash-auto-selection-race/07-VERIFICATION.md
      provides: "Structured verification record for Phase 07"
      contains: "LOCALE-01"
  key_links:
    - from: "Kiosk Windows locale = English-US"
      to: "Magicline UI rendered in German"
      via: "app.commandLine --lang + webRequest Accept-Language"
      pattern: "lang=de-DE"
    - from: "welcome:tap"
      to: "auto-select.result=ok audit line"
      via: "inject.js state machine → sentinel → magiclineView.js → log.audit"
      pattern: "event=auto-select\\.result result=ok"
---

<objective>
Human verification that Phase 07 actually achieves its goal on the live kiosk. This is a
non-autonomous plan — the executor prepares checklists and the user runs them over RDP
against the Win 11 Pro kiosk with its display language deliberately flipped to English-US.

Purpose: The changes in Plans 01-05 are unobservable in dev-mode. The only honest
verification of the locale race and the splash gate is on the real hardware where the
Magicline SPA, the touch panel, and the live network all interact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-locale-hardening-splash-auto-selection-race/07-CONTEXT.md
@.planning/phases/07-locale-hardening-splash-auto-selection-race/07-RESEARCH.md §10
@.planning/phases/07-locale-hardening-splash-auto-selection-race/07-01-SUMMARY.md
@.planning/phases/07-locale-hardening-splash-auto-selection-race/07-02-SUMMARY.md
@.planning/phases/07-locale-hardening-splash-auto-selection-race/07-03-SUMMARY.md
@.planning/phases/07-locale-hardening-splash-auto-selection-race/07-04-SUMMARY.md
@.planning/phases/07-locale-hardening-splash-auto-selection-race/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the kiosk-visit checklist and create 07-VERIFICATION.md stub</name>
  <files>docs/runbook/v1.1-KIOSK-VISIT.md, .planning/phases/07-locale-hardening-splash-auto-selection-race/07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-locale-hardening-splash-auto-selection-race/07-RESEARCH.md §10
    - .planning/phases/07-locale-hardening-splash-auto-selection-race/07-CONTEXT.md success criteria
    - docs/runbook/v1.1-KIOSK-VISIT.md (whatever Plan 01 already wrote)
  </read_first>
  <action>
**Edit 1 — Append a Phase 07 verification section to `docs/runbook/v1.1-KIOSK-VISIT.md`.**
After the existing "Phase 07 DOM survey (Wave 0)" section from Plan 01, add:

```markdown
## Phase 07 verification (LOCALE-01 + SPLASH-01)

**Goal:** Prove the locale fix and splash gate work on the live kiosk.
**Prerequisite:** Kiosk Windows display language SET TO ENGLISH-US.
Settings → Time & Language → Language → Windows display language → English (United States). Reboot.

### L1 — Locale on English Windows
- [ ] After kiosk restart, welcome layer tap → login completes → Magicline UI renders in German (not English).
- [ ] DevTools (dev-mode build) on Magicline view: `navigator.language` === `'de-DE'`
- [ ] DevTools Network tab on first request to `bee-strong-fitness.web.magicline.com`: `Accept-Language: de-DE,de;q=0.9`
- [ ] `%AppData%\Bee Strong POS\logs\` latest audit line contains `event=startup.locale lang=de`

### L2 — Auto-select happy-path cycles
- [ ] Complete 5 consecutive cycles of: welcome tap → splash → auto-select chain → cash register ready → idle timeout → welcome.
- [ ] Each cycle emits ONE audit line matching `event=auto-select.result result=ok`.
- [ ] No cycle emits `result=fail` or `result=timeout`.
- [ ] Zero cycles leave the splash stuck.

### S1 — Splash pointer block
- [ ] During the ~1-2 s window between welcome:tap and cash register ready, deliberately tap random positions on the splash with a finger.
- [ ] During the same auto-select pending window, DevTools-inspect the splash layer DOM node (`.bsk-layer--splash`) and confirm it has class `auto-select-pending` applied.
- [ ] No tap reaches the underlying Magicline view (no Magicline UI interaction / no focus change / no button highlight).
- [ ] Auto-select chain still completes (sentinel audit line `result=ok` emitted).

### S2 — Splash safety timeout (forced failure)
- [ ] In dev-mode DevTools, right before Step 3 fires, remove the `Self-Checkout` option element from the DOM:
  ```js
  document.querySelectorAll('[role="option"]').forEach(o => { if (o.textContent.trim() === 'Self-Checkout') o.remove(); });
  ```
- [ ] Observe audit log emits `event=auto-select.result result=fail step=step3-self-checkout` within 1.5 s.
- [ ] Splash hides via the degraded path (visible within ≤5500 ms of the original welcome:tap).
- [ ] Kiosk lands on the manual register picker; user can recover manually.

### S3 — Admin PIN reachability during pending state
- [ ] During the auto-select pending window, press Ctrl+Shift+F12.
- [ ] Admin PIN modal appears above the splash and accepts input.
- [ ] Splash-pending state does NOT block the admin PIN modal.

### R1 — Cold-boot regression
- [ ] Full kiosk reboot. After boot, welcome layer renders immediately, splash is NOT stuck pending.
- [ ] Cold-boot splash:hide still fires (legacy path unchanged).
- [ ] `event=startup.complete` audit line present.

### R2 — Idle-recovery regression
- [ ] From cash register, wait for the 60 s idle to fire. Tap the idle overlay to dismiss.
- [ ] Kiosk returns via sessionReset.hardReset (reason=idle-expired).
- [ ] New welcome cycle runs cleanly — splash gate re-arms normally.

### Post-test restoration
- [ ] Set Windows display language back to German (production default).
- [ ] Reboot kiosk to confirm German-Windows path still works.

**Sign-off:** _______________  **Date:** _______________
```

**Edit 2 — Create `.planning/phases/07-locale-hardening-splash-auto-selection-race/07-VERIFICATION.md`:**

```markdown
# Phase 07 Verification — Locale Hardening & Splash Auto-Selection Race

**Phase:** 07-locale-hardening-splash-auto-selection-race
**Status:** Pending kiosk visit
**Requirements:** LOCALE-01, SPLASH-01

## Automated verification (runs on every CI / local `node --test`)

| Test file | Requirement | Expected |
|-----------|-------------|----------|
| test/fragileSelectors.test.js | LOCALE-01 | LOCALE_STRINGS.de shape correct, 3 keys non-empty |
| test/magiclineView.sentinel.test.js | LOCALE-01 / SPLASH-01 | parseAutoSelectSentinel allowlist, 8 cases pass |
| test/logger.audit.test.js | LOCALE-01 | Existing log.audit format regression — auto-select.result emits canonical format |

Run: `node --test test/fragileSelectors.test.js test/magiclineView.sentinel.test.js test/logger.audit.test.js`

## Manual kiosk verification

See `docs/runbook/v1.1-KIOSK-VISIT.md` → Phase 07 verification section.

| Check ID | Description | Pass / Fail / Blocked | Date | Notes |
|----------|-------------|-----------------------|------|-------|
| L1 | Locale de-DE on English Windows | | | |
| L2 | 5 happy-path cycles all emit result=ok | | | |
| S1 | Splash pointer block swallows member taps | | | |
| S2 | Forced failure degrades within 5500 ms | | | |
| S3 | Admin PIN reachable during pending state | | | |
| R1 | Cold-boot path unchanged | | | |
| R2 | Idle-recovery path unchanged | | | |

## Gaps / open items
(filled after kiosk visit)
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('docs/runbook/v1.1-KIOSK-VISIT.md','utf8'); const v=fs.readFileSync('.planning/phases/07-locale-hardening-splash-auto-selection-race/07-VERIFICATION.md','utf8'); for (const needle of ['Phase 07 verification','L1 — Locale','L2 — Auto-select','S1 — Splash pointer','S2 — Splash safety','S3 — Admin PIN','R1 — Cold-boot','R2 — Idle-recovery']) { if (!r.includes(needle)) throw new Error('runbook missing: '+needle); } for (const needle of ['LOCALE-01','SPLASH-01','fragileSelectors.test.js','magiclineView.sentinel.test.js']) { if (!v.includes(needle)) throw new Error('verification missing: '+needle); } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - docs/runbook/v1.1-KIOSK-VISIT.md contains all check IDs: L1, L2, S1, S2, S3, R1, R2
    - 07-VERIFICATION.md exists and lists both requirements LOCALE-01 and SPLASH-01
    - 07-VERIFICATION.md lists all 3 test files (fragileSelectors, magiclineView.sentinel, logger.audit)
  </acceptance_criteria>
  <done>Checklists authored; ready for kiosk walk.</done>
</task>

<task type="auto">
  <name>Task 1b: Run Phase 07 unit-test gate (MUST pass before human kiosk walk)</name>
  <files>(read-only: test/fragileSelectors.test.js, test/magiclineView.sentinel.test.js, test/logger.audit.test.js)</files>
  <action>
Mechanical gate that MUST pass before the human kiosk checkpoint (Task 2) fires. Runs the three Phase 07 unit test files produced by Plans 01/02/03 and asserts all three file paths appear in the `node --test` stdout.
  </action>
  <verify>
    <automated>node --test test/fragileSelectors.test.js test/magiclineView.sentinel.test.js test/logger.audit.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/fragileSelectors.test.js test/magiclineView.sentinel.test.js test/logger.audit.test.js` exits with code 0
    - stdout contains the path `test/fragileSelectors.test.js`
    - stdout contains the path `test/magiclineView.sentinel.test.js`
    - stdout contains the path `test/logger.audit.test.js`
    - No failing subtests reported
  </acceptance_criteria>
  <done>All three Phase 07 unit test files green; human kiosk walk (Task 2) may proceed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run kiosk visit checklist over RDP — fill pass/fail results</name>
  <files>.planning/phases/07-locale-hardening-splash-auto-selection-race/07-VERIFICATION.md</files>
  <what-built>
    All code changes from Plans 02-05 are live in the current build. Task 1 of this plan
    has authored `docs/runbook/v1.1-KIOSK-VISIT.md` Phase 07 section and
    `07-VERIFICATION.md`. The user must now run the checklist on the live kiosk over RDP.
  </what-built>
  <how-to-verify>
    1. Ensure a production build (or dev build with all Plan 02-05 changes) is deployed to the kiosk via `/gsd-execute-phase` followup or manual RDP install.
    2. Flip Windows display language on the kiosk to English-US. Reboot kiosk.
    3. Work through the checklist in `docs/runbook/v1.1-KIOSK-VISIT.md` → Phase 07 verification section, checks L1 through R2.
    4. Record pass/fail for each in the `07-VERIFICATION.md` table with notes where any step degrades.
    5. If ANY check fails: stop, gather logs from `%AppData%\Bee Strong POS\logs\`, open a gap closure task via `/gsd-plan-phase 07 --gaps`, and do NOT mark Phase 07 complete.
    6. On full pass: flip Windows display language back to German, reboot, confirm one more cycle works, commit the filled-in verification doc.
  </how-to-verify>
  <acceptance_criteria>
    - 07-VERIFICATION.md table has all 7 check IDs filled with Pass/Fail/Blocked
    - If any row is Fail, a gap-closure plan number is referenced in the notes
    - Windows language restored to German before ending the session
  </acceptance_criteria>
    <action>See &lt;how-to-verify&gt; above — this is a human verification checkpoint; the executor has no autonomous action beyond writing the files the checkpoint references.</action>
  <verify><automated>test -f docs/runbook/v1.1-KIOSK-VISIT.md</automated></verify>
  <done>Human tester has filled in the checklist rows and typed the resume-signal.</done>
  <resume-signal>Type "phase07-verified pass" or "phase07-verified fail gaps=<list>"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tester → kiosk → audit log | Trusted path; verification is what closes every T-07-* mitigation claim from Plans 02-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Tampering (unverified mitigation) | All T-07-* threats from Plans 02-05 claim "mitigate" without live-kiosk evidence | mitigate | This plan's checks L1-R2 directly exercise each mitigation (L1/L2 → T-07-03/04, S1 → T-07-09, S2 → T-07-02/10/11, S3 → T-07-13, R1/R2 → T-07-06). Only after all 7 checks pass is Phase 07 "secure-by-verification", not just "secure-by-design". |
</threat_model>

<verification>
- docs/runbook/v1.1-KIOSK-VISIT.md check IDs L1-R2 grep-present
- 07-VERIFICATION.md exists with all filled rows after kiosk walk
- All automated unit tests from prior plans still green
</verification>

<success_criteria>
- Phase 07 unit-test gate (Task 1b) exits 0: `node --test test/fragileSelectors.test.js test/magiclineView.sentinel.test.js test/logger.audit.test.js`
- All 7 manual checks Pass on English-US Windows
- Audit logs show `event=auto-select.result result=ok` for 5 consecutive cycles
- No cold-boot or idle-recovery regression
- Windows language restored to German for production use
</success_criteria>

<output>
After completion, create `.planning/phases/07-locale-hardening-splash-auto-selection-race/07-06-SUMMARY.md`
</output>
