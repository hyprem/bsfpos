---
phase: 06-welcome-screen-lifecycle-redesign
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/main/main.js
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, IDLE-03, IDLE-04]
must_haves:
  truths:
    - "On cold boot after app.whenReady + mainWindow creation + splash, the host is commanded to show the welcome layer; Magicline view is NOT created until first tap"
    - "welcome:tap IPC from the renderer causes main to hide welcome, show splash, create Magicline view, start authFlow"
    - "authFlow.start is only called AFTER a Magicline view exists"
    - "Second and subsequent taps re-enter the same create-and-start path after welcome-mode hardReset destroyed the view"
    - "Credentials overlay path still works: if creds are missing on first tap, showCredentialsOverlay fires"
    - "welcome-mode hardReset completion causes the renderer to receive welcome:show (sent by sessionReset.js Plan 06-02) — main does not need to re-send"
    - "splash:hide is sent before the cold-boot welcome:show so the welcome layer becomes visible without a Magicline did-finish-load"
  artifacts:
    - path: "src/main/main.js"
      provides: "cold-boot-to-welcome orchestration + welcome:tap IPC handler"
      contains: "ipcMain.on('welcome:tap'"
  key_links:
    - from: "src/main/main.js (app.whenReady)"
      to: "mainWindow.webContents.send('welcome:show')"
      via: "after did-finish-load, before any authFlow.start"
      pattern: "welcome:show"
    - from: "src/main/main.js (ipcMain welcome:tap handler)"
      to: "createMagiclineView + authFlow.start"
      via: "fire-once-per-tap orchestration"
      pattern: "welcome:tap"
---

<objective>
Rewire main.js so cold boot lands on the welcome layer (no pre-warmed Magicline view), and a
welcome:tap IPC from the host triggers the full login flow (create Magicline view + authFlow.start).
Keep post-welcome-mode-reset working: when sessionReset.hardReset with mode welcome completes,
sessionReset itself sends welcome:show (from Plan 06-02). Main only ensures no other code path
auto-recreates the view.

Purpose: realises D-03 (cold boot lands on welcome) and the orchestration half of D-05 (welcome
tap triggers new view + authFlow). Consumes the IPC surface from Plan 06-01 and the sessionReset
welcome branch from Plan 06-02.

Output: a booted kiosk sits on the welcome layer until first tap; tapping triggers the existing
authFlow path; idle expiry + 10s countdown drives welcome-mode hardReset; welcome layer reappears;
next tap repeats the cycle. No more in-place idle-reset-then-auto-login.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-welcome-screen-lifecycle-redesign/06-CONTEXT.md
@.planning/phases/06-welcome-screen-lifecycle-redesign/06-01-PLAN.md
@.planning/phases/06-welcome-screen-lifecycle-redesign/06-02-PLAN.md
@src/main/main.js
@src/main/authFlow.js
@src/main/magiclineView.js

<interfaces>
Current cold-boot orchestration lives in src/main/main.js inside the app.whenReady block, roughly
lines 358 through 453. It currently calls `createMagiclineView(mainWindow, store)` unconditionally,
then defines a local `startAuthFlow` arrow and invokes it on did-finish-load.

Phase 6 cold-boot must SKIP createMagiclineView and startAuthFlow on app launch. Both fire inside
a new `ipcMain.on('welcome:tap', ...)` handler instead. tryInitAutoUpdater + autoUpdater.checkForUpdates
+ startUpdateCheckInterval are INDEPENDENT of the Magicline view — they still run at cold-boot time.

Constraints:
- authFlow.start requires a non-destroyed webContents — must be called AFTER createMagiclineView.
- The health watchdog and preReset/postReset wiring and admin hotkey handler registration stay at
  cold-boot time (module-lifetime, not per-tap).
- setAdminHotkeyHandler is re-applied by magiclineView on every createMagiclineView call (Phase 5
  WR-01 note), so registering it once at cold boot is still correct.
- sessionReset.js after Plan 06-02 sends welcome:show itself on welcome-mode completion — main.js
  does NOT re-send it after hardReset returns.
- Cold-boot first-time welcome:show is sent FROM main.js because no reset has happened yet.
- The host splash layer currently hides on the Magicline did-finish-load event. With no Magicline
  view at cold boot, splash would never auto-hide — so main must explicitly send splash:hide
  before the cold-boot welcome:show.

Credentials overlay path (no change needed):
- authFlow.start synchronously calls notify with creds-loaded or safestorage-unavailable or
  decrypt-failed. On first run with no creds, the reducer surfaces the credentials overlay via
  show-credentials-overlay IPC. The credentials overlay is layer 400, above splash (layer 100) and
  welcome (layer 150), so it works regardless of which lower layer is visible.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gate cold-boot view creation behind welcome:tap</name>
  <read_first>
    - src/main/main.js (full file — focus on lines 260-460 for the cold-boot block, and lines 520-560 for the existing ipcMain.on handlers area)
    - src/main/authFlow.js (lines 484-510 for start() signature)
    - src/main/magiclineView.js (lines 116-125 for createMagiclineView signature + the already-created guard)
    - .planning/phases/06-welcome-screen-lifecycle-redesign/06-CONTEXT.md (D-03, D-05)
    - .planning/phases/06-welcome-screen-lifecycle-redesign/06-01-PLAN.md (IPC channel names)
    - .planning/phases/06-welcome-screen-lifecycle-redesign/06-02-PLAN.md (sessionReset emits welcome:show itself on welcome-mode completion)
  </read_first>
  <files>src/main/main.js</files>
  <action>
In src/main/main.js, inside the `if (mainWindow)` block of app.whenReady, refactor the cold-boot
flow. The goal is twofold: (1) do NOT create the Magicline view at cold boot, and (2) add a
welcome:tap IPC handler that creates the view + starts authFlow on demand.

**Marker discipline (Phase 1 D-01):** All edits in this task land BELOW the existing `// === ORCHESTRATION ===` marker inside the `app.whenReady` `if (mainWindow)` block. Do NOT mutate `createMainWindow` — the orchestration region is the ONLY place where per-boot wiring belongs. Before editing, grep for the marker: `grep -n "ORCHESTRATION" src/main/main.js` to confirm its location.

Step A — Replace the existing cold-boot view creation and startAuthFlow definition+invocation.

DELETE the block that currently reads roughly like this (around lines 408-453):

```
const magiclineView = createMagiclineView(mainWindow, store);
log.info('phase2.magicline-view.created');

const startAuthFlow = () => {
  try {
    authFlow.start({ mainWindow, webContents: magiclineView.webContents, store, safeStorage, log });
    log.info('phase3.authFlow.started');
    try {
      const ok = tryInitAutoUpdater(store);
      if (ok) { autoUpdater.checkForUpdates(); startUpdateCheckInterval(); }
    } catch (err) { log.error('phase5.autoUpdater.init failed: ' + (err && err.message)); }
  } catch (err) {
    log.error('phase3.authFlow.start failed: ' + (err && err.message));
  }
};
if (mainWindow.webContents.isLoading()) {
  mainWindow.webContents.once('did-finish-load', startAuthFlow);
} else {
  startAuthFlow();
}
```

REPLACE with the following block (preserve the existing `setAdminHotkeyHandler(openAdminPinModal)`
call IMMEDIATELY above it — leave that line intact):

```
// Phase 6 D-03: cold boot does NOT create the Magicline view. Instead, main
// commands the host to show the welcome layer. The Magicline view is created
// lazily on the first welcome:tap (see ipcMain.on('welcome:tap') below), and
// recreated automatically after any non-welcome hardReset (sessionReset.js
// mode:'reset' branch still calls createMagiclineView). Welcome-mode hardReset
// (Plan 06-02) keeps the view destroyed and re-shows welcome itself.

// Helper: starts (or restarts) the login flow. Ensures a Magicline view exists,
// then kicks authFlow. Safe to call multiple times — createMagiclineView is
// idempotent (returns the existing instance if one is already attached), and
// authFlow.start re-seeds currentState to BOOTING and reloads credentials.
const startLoginFlow = () => {
  try {
    const view = createMagiclineView(mainWindow, store);
    log.info('phase6.login-flow.view-ready');
    authFlow.start({
      mainWindow: mainWindow,
      webContents: view.webContents,
      store: store,
      safeStorage: safeStorage,
      log: log,
    });
    log.info('phase3.authFlow.started');
  } catch (err) {
    log.error('phase6.startLoginFlow failed: ' + (err && err.message));
  }
};

// Cold-boot auto-updater init is INDEPENDENT of the Magicline view and still
// runs at app.whenReady time (not per welcome:tap). Preserve Phase 5 wiring.
const runAutoUpdaterInit = () => {
  try {
    const ok = tryInitAutoUpdater(store);
    if (ok) {
      autoUpdater.checkForUpdates();
      startUpdateCheckInterval();
    }
  } catch (err) {
    log.error('phase5.autoUpdater.init failed: ' + (err && err.message));
  }
};

// Cold-boot welcome command — deferred until the host renderer is ready so
// the IPC is not dropped. Same did-finish-load pattern as the old Phase 3
// startAuthFlow timing guard. MUST send splash:hide before welcome:show
// because the Phase 1 splash layer currently hides only on Magicline
// did-finish-load, and no Magicline view exists at cold boot.
const showWelcomeOnColdBoot = () => {
  try {
    mainWindow.webContents.send('splash:hide');
    mainWindow.webContents.send('welcome:show');
    log.info('phase6.cold-boot.welcome-shown');
  } catch (err) {
    log.error('phase6.cold-boot.welcome:show failed: ' + (err && err.message));
  }
  runAutoUpdaterInit();
};
if (mainWindow.webContents.isLoading()) {
  mainWindow.webContents.once('did-finish-load', showWelcomeOnColdBoot);
} else {
  showWelcomeOnColdBoot();
}
```

Step B — Add the welcome:tap IPC handler. Place it alongside the existing idle-dismissed /
idle-expired / request-reset-loop-recovery handlers (around line 534). Add exactly this block:

```
// Phase 6 D-02 / D-03 — welcome:tap IPC.
// User tapped the welcome layer. Hide welcome, show splash as a loading cover
// while the ~3-5s login flow runs, create the Magicline view, start authFlow.
// Idempotent: if a view already exists (e.g. double-tap), createMagiclineView
// returns the existing instance and authFlow.start re-seeds currentState.
ipcMain.on('welcome:tap', (ev) => {
  // Sender validation — only trust the host mainWindow webContents.
  if (ev.sender !== mainWindow.webContents) {
    log.warn('phase6.welcome:tap from unknown sender — ignored');
    return;
  }
  log.info('phase6.welcome:tap received — starting login flow');
  try {
    mainWindow.webContents.send('welcome:hide');
    mainWindow.webContents.send('splash:show');
  } catch (err) {
    log.error('phase6.welcome:tap send failed: ' + (err && err.message));
  }
  startLoginFlow();
});
```

Step C — Do NOT touch any of the following:
- sessionResetMod.onPreReset wiring (WR-08 health watchdog clear + re-arm).
- setAdminHotkeyHandler(openAdminPinModal) — still called once at cold boot, immediately above the
  refactored block.
- require('./sessionReset').init and require('./idleTimer').init.
- startHealthWatchdog(store).
- The existing ipcMain.handle submit-credentials, verify-pin, idle-dismissed, idle-expired,
  request-reset-loop-recovery, admin menu handlers, PAT config handlers, and the audit-sale-completed
  listener.
- The render-process-gone recovery in magiclineView (still calls hardReset with no mode, so the
  default 'reset' branch fires, and view is recreated — correct).

Trace the first-run-no-creds path after the edit:
1. Cold boot → splash visible → splash:hide + welcome:show sent → welcome layer visible.
2. User taps → welcome:tap → main sends welcome:hide + splash:show → createMagiclineView +
   authFlow.start.
3. authFlow detects no creds → show-credentials-overlay IPC → host shows overlay over splash.
4. User submits creds → authFlow persists + transitions to BOOTING → Magicline loads → cash
   register ready → splash:hide fires via the existing Magicline did-finish-load path.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/main/main.js','utf8');const checks=[['welcome:show cold-boot',/welcome:show/],['welcome:tap handler',/ipcMain\.on\('welcome:tap'/],['startLoginFlow helper',/startLoginFlow/],['welcome:hide on tap',/welcome:hide/],['splash:hide cold-boot',/splash:hide/],['sender validation',/ev\.sender !== mainWindow\.webContents/],['runAutoUpdaterInit',/runAutoUpdaterInit/]];let ok=true;for(const[l,r]of checks){if(!r.test(s)){console.error('MISSING',l);ok=false}}if(!ok)process.exit(1);console.log('patterns ok')" &amp;&amp; npm test --silent</automated>
  </verify>
  <acceptance_criteria>
    - main.js contains a `startLoginFlow` helper that calls createMagiclineView then authFlow.start
    - main.js contains `ipcMain.on('welcome:tap', ...)` that validates sender and calls startLoginFlow
    - Cold-boot path sends `splash:hide` then `welcome:show` inside a did-finish-load guard
    - `runAutoUpdaterInit` runs at cold boot (auto-updater init preserved)
    - No top-level `createMagiclineView(mainWindow, store)` call outside startLoginFlow inside the app.whenReady block
    - sessionResetMod.onPreReset hooks, setAdminHotkeyHandler, init calls, and all existing ipcMain handlers are preserved verbatim
    - `npm test` full suite passes with zero regressions
  </acceptance_criteria>
  <done>Cold boot lands on welcome; welcome:tap creates view + runs authFlow; cycle repeats indefinitely; existing tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host renderer → main (welcome:tap) | Untrusted renderer triggers main-side view creation + credential-consuming authFlow |
| main → Magicline child wc | New view spun up per tap; auth state machine injects credentials into it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Spoofing | welcome:tap IPC sender | mitigate | Handler validates `ev.sender !== mainWindow.webContents` and logs+drops unknown senders; only the host (not the Magicline child view) can trigger welcome:tap |
| T-06-13 | Denial of Service | Rapid welcome:tap flood creating duplicate views | mitigate | createMagiclineView is idempotent (early-return if `magiclineView` module state is already set) per src/main/magiclineView.js lines 116-122; authFlow.start is safe to re-invoke — it resets currentState to BOOTING |
| T-06-14 | Repudiation | Lost audit trail of who initiated each login session | mitigate | `log.info('phase6.welcome:tap received — starting login flow')` records every tap; Phase 5 log.audit('startup', ...) already covers cold boot; per-tap audit is informational and does not weaken the audit story |
| T-06-15 | Information Disclosure | Credentials loaded into a new wc per tap without cleanup | accept | authFlow uses the existing Phase 3 safeStorage decrypt path which never touches disk plaintext; credentials live in process memory only for the duration of the auto-fill |
| T-06-16 | Tampering | welcome:tap firing before splash/welcome layers exist in the DOM | mitigate | Cold-boot welcome:show is deferred until `did-finish-load`; welcome:tap can only be fired AFTER host.js has subscribed to it, which also happens after DOMContentLoaded |
| T-06-17 | Elevation of Privilege | welcome:tap bypassing auto-updater safe-window gate | accept | welcome:tap triggers only createMagiclineView + authFlow.start; it does NOT call autoUpdater.quitAndInstall. The updateGate post-reset hook still fires on hardReset mode='reset' paths and is not affected by welcome mode |
</threat_model>

<verification>
Full local run:
1. `npm start` — splash appears, then welcome layer (yellow "Zum Kassieren tippen" on dark background).
2. Click anywhere on welcome — splash reappears, Magicline view is created behind it, authFlow
   drives through to cash register, splash hides.
3. Wait 60s with no input — "Noch da?" overlay appears with countdown starting at 10.
4. Let it expire (no interaction) — idleTimer.expired fires → sessionReset welcome-mode runs →
   Magicline view destroyed → welcome:show emitted by sessionReset → welcome layer visible again.
5. Tap welcome again — back to cash register via fresh login (full 3-5s).
6. Repeat steps 3-5 at least five times — no reset-loop error trips, no stale session bleed-through.
</verification>

<success_criteria>
Main.js cold-boot orchestration now ends at welcome, and welcome:tap is the sole entry into the
login flow. Subsequent welcome-mode resets flow sessionReset → welcome:show → host welcome layer
without any main.js involvement. Existing Phase 1-5 wiring (health watchdog, updateGate, admin
hotkey, auto-updater) is preserved verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/06-welcome-screen-lifecycle-redesign/06-03-SUMMARY.md`.
</output>
