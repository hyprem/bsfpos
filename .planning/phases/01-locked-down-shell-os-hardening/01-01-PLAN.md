---
phase: 01-locked-down-shell-os-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - src/main/logger.js
  - src/host/assets/logo-dark.png
  - src/host/assets/logo-light.png
  - README.md
autonomous: true
requirements:
  - SHELL-01
tags:
  - electron
  - bootstrap
  - node

must_haves:
  truths:
    - "A fresh clone can run `npm install` successfully on Windows with Node >= 20"
    - "Project directory layout exists and matches the agreed conventions (src/main, src/host, src/host/assets, docs/runbook, build)"
    - "electron-log writes boot events to %AppData%/Bee Strong POS/logs/main.log"
  artifacts:
    - path: "package.json"
      provides: "Electron project manifest with pinned deps and npm scripts"
      contains: "\"electron\": \"~41.1"
    - path: ".gitignore"
      provides: "Excludes node_modules, dist, logs, and local artifacts"
      contains: "node_modules"
    - path: "src/main/logger.js"
      provides: "Configured electron-log instance with file rotation"
      exports: ["log"]
    - path: "src/host/assets/logo-dark.png"
      provides: "Dark-background Bee Strong logo for splash"
    - path: "src/host/assets/logo-light.png"
      provides: "Light-background Bee Strong logo for future light surfaces"
  key_links:
    - from: "package.json scripts.start"
      to: "electron entry point src/main/main.js (created in plan 02)"
      via: "cross-env NODE_ENV=development electron ."
      pattern: "NODE_ENV=development"
    - from: "src/main/logger.js"
      to: "%AppData%/Bee Strong POS/logs/"
      via: "electron-log file transport"
      pattern: "electron-log"
---

<objective>
Bootstrap the Electron 41 project skeleton: package.json with pinned dependencies, directory layout, .gitignore, electron-log configured with rotating file transport, brand assets copied into place, and npm scripts that later plans will invoke. No Electron entry point is wired yet — that is plan 02.

Purpose: Establish a reproducible greenfield project state so plans 02/03/04 can run in parallel without fighting over package.json, deps, or folder layout.

Output: Installable project (npm install succeeds), logger module ready for import, assets in place, directory skeleton created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-locked-down-shell-os-hardening/01-CONTEXT.md
@.planning/phases/01-locked-down-shell-os-hardening/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json with pinned deps and directory skeleton</name>
  <files>
    package.json
    .gitignore
    src/main/.gitkeep
    src/host/.gitkeep
    src/host/assets/.gitkeep
    build/.gitkeep
    docs/runbook/.gitkeep
    README.md
  </files>
  <read_first>
    CLAUDE.md (to confirm Electron ^41.1.1, electron-builder ^26.8.1, electron-log ^5.2.x, no TypeScript, no bundler, CommonJS main)
    .planning/phases/01-locked-down-shell-os-hardening/01-RESEARCH.md lines 66-92 (Standard Stack, Phase 1 minimum deps, installation block)
    .planning/PROJECT.md (constraints — do not propose alternatives to the prescriptive stack)
    package.json (if already present — read current state before overwriting)
  </read_first>
  <action>
Create `package.json` with the following exact content (CommonJS — do NOT add `"type": "module"`; electron-store 10.x pin per CLAUDE.md requires CJS):

```json
{
  "name": "bee-strong-pos",
  "version": "0.1.0",
  "description": "Self-service POS kiosk for Bee Strong Fitness — Electron shell around Magicline cash register",
  "author": "Bee Strong Fitness",
  "license": "UNLICENSED",
  "private": true,
  "main": "src/main/main.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development electron .",
    "start:prod": "electron .",
    "build": "electron-builder --win",
    "build:dir": "electron-builder --win --dir",
    "postinstall": "electron-builder install-app-deps"
  },
  "dependencies": {
    "electron-log": "~5.2.0"
  },
  "devDependencies": {
    "electron": "~41.1.1",
    "electron-builder": "~26.8.1",
    "cross-env": "~7.0.3"
  }
}
```

Do NOT add a `build` (electron-builder) block yet — plan 04 owns it. Do NOT add electron-store, electron-updater, or safeStorage — those are Phase 3/5.

Create `.gitignore` with exact content:

```
# dependencies
node_modules/

# build output
dist/
out/
release/

# logs
logs/
*.log

# electron-builder cache
.cache/

# editor / os
.vscode/
.idea/
.DS_Store
Thumbs.db

# local env overrides
.env
.env.local
```

Create these empty directories with `.gitkeep` files (so the skeleton is committable):
- `src/main/.gitkeep`
- `src/host/.gitkeep`
- `src/host/assets/.gitkeep`
- `build/.gitkeep` (for installer.nsh in plan 04)
- `docs/runbook/.gitkeep` (for plan 05)

Create `README.md` with a minimal header so the repo has a landing page:

```markdown
# Bee Strong POS Kiosk

Self-service POS kiosk for Bee Strong Fitness.
See `.planning/PROJECT.md` and `CLAUDE.md` for project details.

## Development

```bash
npm install
npm start       # dev mode (windowed, DevTools open)
npm run build   # produces Windows NSIS installer in dist/
```

Operator runbook: `docs/runbook/README.md`.
```

Run `npm install` and verify it completes without error. Verify `node_modules/electron/package.json` shows version starting with `41.1`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(!p.dependencies['electron-log'])process.exit(1);if(!p.devDependencies.electron||!p.devDependencies.electron.startsWith('~41.1'))process.exit(2);if(p.main!=='src/main/main.js')process.exit(3);console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists with `"main": "src/main/main.js"`
    - `package.json` devDependencies contains `electron: ~41.1.1` and `electron-builder: ~26.8.1`
    - `package.json` dependencies contains `electron-log: ~5.2.0`
    - `package.json` scripts has `start` equal to `cross-env NODE_ENV=development electron .`
    - `package.json` does NOT contain `"type": "module"`
    - `.gitignore` exists and contains the literal lines `node_modules/` and `dist/`
    - Directories exist: `src/main/`, `src/host/`, `src/host/assets/`, `build/`, `docs/runbook/`
    - `node_modules/electron/package.json` exists after `npm install` and its `version` field matches `^41\.1\.` (regex)
    - `README.md` exists and mentions `npm start` and `docs/runbook/README.md`
  </acceptance_criteria>
  <done>`npm install` completes cleanly, Electron 41.1.x is resolved, directory skeleton committable to git.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create electron-log logger module and copy brand assets</name>
  <files>
    src/main/logger.js
    src/host/assets/logo-dark.png
    src/host/assets/logo-light.png
  </files>
  <read_first>
    src/main/logger.js (if already exists)
    .planning/phases/01-locked-down-shell-os-hardening/01-RESEARCH.md lines 66-92 (electron-log version pin)
    CLAUDE.md electron-log section ("Zero-dependency, built-in file rotation via maxSize (default 1 MB, old file becomes main.old.log). Writes to %AppData%/Bee Strong POS/logs/")
    1 BSF_vertical.png (confirm exists at repo root)
    3 BSF_vertical_for dark BG.png (confirm exists at repo root)
    .planning/phases/01-locked-down-shell-os-hardening/01-UI-SPEC.md lines 144-160 (logo usage contract)
  </read_first>
  <action>
Create `src/main/logger.js` with exact content:

```javascript
// src/main/logger.js
// Electron-log instance configured for Phase 1 boot events.
// File transport writes to %AppData%/Bee Strong POS/logs/main.log
// Rotation: 1 MB per file (electron-log default), one main.old.log backup.
// This module is the single logging entry point for the main process — all
// future phases import from here.

const log = require('electron-log/main');

// Initialize main-process logging. electron-log v5 requires explicit init.
log.initialize();

// File transport: 1 MB max size, rotation to main.old.log
log.transports.file.level = 'info';
log.transports.file.maxSize = 1024 * 1024; // 1 MB per file
log.transports.file.format = '[{y}-{m}-{d} {h}:{i}:{s}.{ms}] [{level}] {text}';
// Filename defaults to main.log; resolve path on demand so electron app path is ready.
log.transports.file.fileName = 'main.log';

// Console transport: always-on at info in dev, warn in prod
log.transports.console.level = process.env.NODE_ENV === 'development' ? 'debug' : 'warn';
log.transports.console.format = '[{h}:{i}:{s}.{ms}] [{level}] {text}';

module.exports = log;
```

Copy brand assets into the project source tree:
- Copy `1 BSF_vertical.png` (repo root) → `src/host/assets/logo-light.png`
- Copy `3 BSF_vertical_for dark BG.png` (repo root) → `src/host/assets/logo-dark.png`

Use Node to copy (works in bash on Windows):
```bash
node -e "require('fs').copyFileSync('1 BSF_vertical.png','src/host/assets/logo-light.png')"
node -e "require('fs').copyFileSync('3 BSF_vertical_for dark BG.png','src/host/assets/logo-dark.png')"
```

Do NOT modify or re-encode the PNGs — copy byte-for-byte.

Remove `src/host/assets/.gitkeep` once both PNGs exist (the directory is no longer empty).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(!fs.existsSync('src/main/logger.js'))process.exit(1);if(!fs.existsSync('src/host/assets/logo-dark.png'))process.exit(2);if(!fs.existsSync('src/host/assets/logo-light.png'))process.exit(3);const src=fs.readFileSync('src/main/logger.js','utf8');if(!src.includes(\"require('electron-log/main')\"))process.exit(4);if(!src.includes('maxSize'))process.exit(5);console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/logger.js` exists and contains `require('electron-log/main')` and `log.initialize()`
    - `src/main/logger.js` sets `log.transports.file.maxSize = 1024 * 1024`
    - `src/main/logger.js` exports a log instance via `module.exports = log`
    - `src/host/assets/logo-dark.png` exists and is byte-identical to `3 BSF_vertical_for dark BG.png` (verify via file size comparison)
    - `src/host/assets/logo-light.png` exists and is byte-identical to `1 BSF_vertical.png`
    - `require('./src/main/logger')` loads without throwing (can test once electron-log is installed from task 1)
  </acceptance_criteria>
  <done>Logger module is importable and brand assets are in src/host/assets/ ready for host.html to reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Untrusted package code enters via transitive deps; mitigated by pinning exact minor (~) ranges and committing package-lock.json |
| Repo → committed artifacts | Brand PNGs are static assets, not code execution; low risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json deps | mitigate | Pin electron, electron-builder, electron-log, cross-env with `~` minor ranges; commit package-lock.json in task 1 so reproducible installs catch tampering |
| T-01-02 | Information Disclosure | electron-log file transport | mitigate | Phase 1 logs contain only boot and lifecycle events; .gitignore excludes `logs/` and `*.log` so no log data reaches git. Phase 5 will harden the redaction rules for badge/credential data. |
| T-01-03 | Elevation of Privilege | postinstall script (electron-builder install-app-deps) | accept | Runs locally in dev environment only; electron-builder is first-party tooling per prescribed stack. Not invoked on the kiosk device. |
</threat_model>

<verification>
1. `npm install` completes with exit code 0
2. `node_modules/electron/package.json` version field matches `41.1.*`
3. `src/main/logger.js` loads via `node -e "require('./src/main/logger')"` without crashing
4. Brand assets exist at both paths and match source PNG file sizes
5. `.gitignore` excludes node_modules, dist, logs
</verification>

<success_criteria>
- Project is installable from a fresh clone via `npm install`
- Electron 41.1.x is the resolved runtime
- electron-log is importable and configured for 1 MB rotation
- Brand assets present at `src/host/assets/logo-{dark,light}.png`
- Directory skeleton matches the file ownership contract expected by plans 02/03/04
</success_criteria>

<output>
After completion, create `.planning/phases/01-locked-down-shell-os-hardening/01-01-SUMMARY.md`.
</output>
