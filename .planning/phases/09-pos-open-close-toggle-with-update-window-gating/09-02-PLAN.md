---
phase: 09-pos-open-close-toggle-with-update-window-gating
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/host/host.html
  - src/host/host.css
  - src/host/host.js
autonomous: false
requirements:
  - ADMIN-02
user_setup: []

must_haves:
  truths:
    - "Admin menu shows yellow POS schliessen button when posOpen=true, after PIN andern and before Auto-Update einrichten"
    - "Tapping POS schliessen opens a confirm overlay with POS wirklich schliessen? title and Ja/Abbrechen buttons"
    - "Confirming close sends toggle-pos-open IPC, closes overlay, and button becomes green POS offnen"
    - "Tapping POS offnen sends toggle-pos-open IPC immediately with no confirm modal"
    - "When posOpen=false welcome layer shows POS derzeit geschlossen heading and Bitte Studio-Personal verstandigen subtext"
    - "When posOpen=false tapping the welcome screen does NOT fire welcome:tap IPC"
    - "Closing admin menu also hides the pos-close-confirm overlay if open"
    - "Admin diagnostics show POS-Status row with Geoffnet or Geschlossen"
  artifacts:
    - path: "src/host/host.html"
      provides: "POS toggle button, confirm overlay, welcome subtext element"
      contains: "admin-btn-pos-toggle"
    - path: "src/host/host.css"
      provides: "Yellow caution and green safe button variants, welcome subtext, confirm overlay styles"
      contains: "bsk-btn--admin-action--caution"
    - path: "src/host/host.js"
      provides: "applyPosState, updatePosToggleButton, confirm overlay logic, onPosStateChanged subscriber"
      contains: "applyPosState"
  key_links:
    - from: "src/host/host.js"
      to: "src/main/preload.js"
      via: "window.kiosk.onPosStateChanged subscriber"
      pattern: "onPosStateChanged"
    - from: "src/host/host.js"
      to: "src/main/main.js"
      via: "window.kiosk.adminMenuAction('toggle-pos-open')"
      pattern: "adminMenuAction.*toggle-pos-open"
---

<objective>
Implement the host-side UI for POS open/close toggle: admin menu button with yellow/green state variants (per D-01, D-02, D-03), confirm overlay for POS close (per D-02), welcome layer closed-state rendering with tap suppression (per D-05, D-06), pos-state-changed IPC subscriber, and diagnostics row. Includes a human verification checkpoint for the full Phase 09 feature.

Purpose: Delivers the visible admin and member-facing UX for the POS state toggle, wired to Plan 01's main-process backbone.
Output: Modified host.html, host.css, host.js with all Phase 09 UI elements.
</objective>

<execution_context>
@C:\Users\Nico\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Nico\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-CONTEXT.md
@.planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-RESEARCH.md
@.planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-UI-SPEC.md
@.planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-01-SUMMARY.md

<interfaces>
From src/main/preload.js (Plan 01 adds):
```javascript
onPosStateChanged: (cb) => ipcRenderer.on('pos-state-changed', (_e, payload) => cb(payload)),
// payload shape: { posOpen: boolean }
```

From src/main/main.js (Plan 01 adds):
```javascript
// admin-menu-action 'toggle-pos-open' returns: { ok: true, posOpen: boolean }
// buildAdminDiagnostics now includes: posOpen: boolean (default true)
// pos-state-changed IPC sent on: toggle, cold boot, session reset to welcome
```

From src/host/host.html (current admin button stack lines 188-197):
```html
<div class="bsk-admin-btns">
  <button ... id="admin-btn-check-updates">Updates pr&uuml;fen</button>
  <button ... id="admin-btn-logs">Protokolle anzeigen</button>
  <button ... id="admin-btn-reload">Kasse nachladen</button>
  <button ... id="admin-btn-credentials">Anmeldedaten &auml;ndern</button>
  <button ... id="admin-btn-pin-change">PIN &auml;ndern</button>
  <!-- Phase 09 INSERT HERE -->
  <button ... id="admin-btn-update-config">Auto-Update einrichten</button>
  <button ... id="admin-btn-dev-mode" style="display:none;">Dev-Modus</button>
  <button ... id="admin-btn-exit">Beenden</button>
</div>
```

From src/host/host.html (welcome screen lines 39-48):
```html
<div id="welcome-screen" class="bsk-layer bsk-layer--welcome" ...>
  <img src="assets/logo-dark.png" class="bsk-logo" ...>
  <h1 class="bsk-welcome-title">Zum Kassieren tippen</h1>
</div>
```

From src/host/host.js:
```javascript
// renderDiagnostics(d) line 543 — renders admin diagnostics header
// hideAdminMenu() line 567 — hides admin overlay + admin-update-result
// wireAdminButtons() line 786 — maps button IDs to action strings
// handleWelcomeTap(ev) line 93 — fires welcome:tap IPC
// showWelcome() — makes welcome-screen visible
// IPC subscriber block starts line 978
```

From src/host/host.css:
```css
.bsk-btn--admin-action { min-height: 64px; font-size: 18px; font-weight: 700; }
.bsk-btn--admin-exit { background: #FF6B6B; color: #1A1A1A; border-color: #FF6B6B; }
.bsk-btn--admin-exit:active { background: #E05050; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML + CSS — POS toggle button, confirm overlay, welcome subtext, button variants</name>
  <files>src/host/host.html, src/host/host.css</files>
  <read_first>
    - src/host/host.html (full file — admin button stack, welcome-screen, overlay patterns, z-index ladder)
    - src/host/host.css (full file — .bsk-btn patterns, .bsk-card, .bsk-layer--credentials)
    - .planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-UI-SPEC.md (component specs, colors, typography)
    - .planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-CONTEXT.md (D-01, D-02, D-05)
  </read_first>
  <action>
    In src/host/host.html:

    1. Insert POS toggle button AFTER #admin-btn-pin-change, BEFORE #admin-btn-update-config (per D-01). Default state posOpen=true (yellow caution):
       `<button type="button" class="bsk-btn bsk-btn--admin-action bsk-btn--admin-action--caution" id="admin-btn-pos-toggle" aria-label="POS schliessen &mdash; Best&auml;tigung erforderlich">POS schliessen</button>`

    2. Add #pos-close-confirm overlay as sibling layer div (same pattern as #pin-change-overlay: .bsk-layer .bsk-layer--credentials at z-400). Place AFTER existing #pin-change-overlay div. Structure:
       - Outer div: id="pos-close-confirm", class="bsk-layer bsk-layer--credentials", style="display:none;", aria-hidden="true", role="dialog", aria-label="POS schlie&szlig;en best&auml;tigen"
       - Inner .bsk-card containing:
         - h2.bsk-card-title: "POS wirklich schliessen?"
         - p.bsk-confirm-body: "Mitglieder sehen einen geschlossen-Hinweis."
         - div.bsk-card-actions with two buttons:
           - id="pos-confirm-yes", class="bsk-btn bsk-btn--primary": "Ja, schliessen"
           - id="pos-confirm-cancel", class="bsk-btn": "Abbrechen"

    In src/host/host.css (add after existing .bsk-btn--admin-exit rules):

    3. Yellow caution variant:
       `.bsk-btn--admin-action--caution { background: #F5C518; color: #1A1A1A; border-color: #F5C518; }`
       `.bsk-btn--admin-action--caution:active { background: #D9AD10; }`

    4. Green safe variant:
       `.bsk-btn--admin-action--safe { background: #4CAF50; color: #1A1A1A; border-color: #4CAF50; }`
       `.bsk-btn--admin-action--safe:active { background: #3D8B40; }`

    5. Welcome subtext:
       `.bsk-welcome-subtext { font-size: 16px; font-weight: 400; color: #9CA3AF; text-align: center; margin: 8px 0 0 0; line-height: 1.5; max-width: 80vw; }`

    6. Confirm overlay body text:
       `.bsk-confirm-body { font-size: 16px; font-weight: 400; color: #9CA3AF; text-align: center; line-height: 1.5; padding: 0 8px; margin: 0 0 16px 0; }`
  </action>
  <verify>
    <automated>node -e "var fs=require('fs'),h=fs.readFileSync('src/host/host.html','utf8'),c=fs.readFileSync('src/host/host.css','utf8'),ok=true;['admin-btn-pos-toggle','pos-close-confirm','pos-confirm-yes','pos-confirm-cancel'].forEach(function(i){if(!h.includes(i)){console.log('MISS:'+i);ok=false}});['bsk-btn--admin-action--caution','bsk-btn--admin-action--safe','bsk-welcome-subtext','bsk-confirm-body'].forEach(function(i){if(!c.includes(i)){console.log('MISS:'+i);ok=false}});console.log(ok?'PASS':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - src/host/host.html contains `id="admin-btn-pos-toggle"` between #admin-btn-pin-change and #admin-btn-update-config
    - src/host/host.html contains `id="pos-close-confirm"` with class="bsk-layer bsk-layer--credentials" and role="dialog"
    - src/host/host.html contains `id="pos-confirm-yes"` with class including bsk-btn--primary and text "Ja, schliessen"
    - src/host/host.html contains `id="pos-confirm-cancel"` with text "Abbrechen"
    - src/host/host.css contains `.bsk-btn--admin-action--caution` with `background: #F5C518`
    - src/host/host.css contains `.bsk-btn--admin-action--safe` with `background: #4CAF50`
    - src/host/host.css contains `.bsk-welcome-subtext` with `color: #9CA3AF`
    - src/host/host.css contains `.bsk-confirm-body` with `color: #9CA3AF`
  </acceptance_criteria>
  <done>All Phase 09 HTML elements and CSS classes are in place: POS toggle button in correct stack position, confirm overlay with branded card, button color variants, welcome subtext class, confirm body text class.</done>
</task>

<task type="auto">
  <name>Task 2: host.js — POS state rendering, toggle logic, confirm overlay, IPC subscriber, diagnostics</name>
  <files>src/host/host.js</files>
  <read_first>
    - src/host/host.js (full file — renderDiagnostics at line 543, hideAdminMenu at line 567, wireAdminButtons at line 786, handleWelcomeTap at line 93, showWelcome function, IPC subscriber block at line 978, existing Esc keydown handler with nested overlay guard at line 1026)
    - src/host/host.html (as modified by Task 1 — new element IDs to reference)
    - .planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-CONTEXT.md (D-02, D-03, D-05, D-06, D-13)
    - .planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-RESEARCH.md (applyPosState pattern, updatePosToggleButton pattern, Pitfall 2 re-apply on welcome show, Pitfall 3 cleanup on admin close)
    - .planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-UI-SPEC.md (interaction contract, diagnostics row, copywriting)
  </read_first>
  <action>
    In src/host/host.js:

    1. Add a module-level state variable near the top of the IIFE (after existing module-level vars):
       `var posOpenState = true;`

    2. Add `applyPosState(posOpen)` function (place near other welcome-related functions). This mutates #welcome-screen in place per D-05:
       - Get `var el = document.getElementById('welcome-screen');` — return early if null
       - Get `var h1 = el.querySelector('.bsk-welcome-title');`
       - Get `var sub = el.querySelector('.bsk-welcome-subtext');`
       - When posOpen is true (open state):
         - Set h1.textContent to 'Zum Kassieren tippen'
         - Remove sub element if it exists: `if (sub) sub.remove();`
         - Set el.style.pointerEvents to '' (restore default auto from CSS)
         - Set el.style.cursor to ''
         - Set el.setAttribute('role', 'button') and setAttribute('tabindex', '0')
         - Set el.setAttribute('aria-label', 'Zum Kassieren tippen')
       - When posOpen is false (closed state):
         - Set h1.textContent to 'POS derzeit geschlossen'
         - If sub does not exist, create: `sub = document.createElement('p'); sub.className = 'bsk-welcome-subtext'; el.appendChild(sub);`
         - Set sub.textContent to 'Bitte Studio-Personal verst\u00e4ndigen'
         - Set el.style.pointerEvents to 'none'
         - Set el.style.cursor to 'default'
         - el.removeAttribute('role'); el.removeAttribute('tabindex')
         - Set el.setAttribute('aria-label', 'POS geschlossen')
       - Update module-level: `posOpenState = posOpen;`

    3. Add `updatePosToggleButton(posOpen)` function. Gets `var btn = document.getElementById('admin-btn-pos-toggle');` — return early if null.
       - When posOpen is true:
         - btn.textContent = 'POS schliessen'
         - btn.classList.remove('bsk-btn--admin-action--safe')
         - btn.classList.add('bsk-btn--admin-action--caution')
         - btn.setAttribute('aria-label', 'POS schliessen \u2014 Best\u00e4tigung erforderlich')
       - When posOpen is false:
         - btn.textContent = 'POS \u00f6ffnen'
         - btn.classList.remove('bsk-btn--admin-action--caution')
         - btn.classList.add('bsk-btn--admin-action--safe')
         - btn.setAttribute('aria-label', 'POS \u00f6ffnen')

    4. Add `showPosCloseConfirm()` and `hidePosCloseConfirm()` functions:
       - showPosCloseConfirm: get #pos-close-confirm, set display='', aria-hidden='false'
       - hidePosCloseConfirm: get #pos-close-confirm, set display='none', aria-hidden='true'

    5. Extend `renderDiagnostics(d)` (line 543) to add a POS-Status diagnostics row. After the existing diagnostics rendering, add:
       - Create a label span with class bsk-diag-label, textContent 'POS-Status'
       - Create a value span with class bsk-diag-value
       - If d.posOpen is true: value textContent = 'Ge\u00f6ffnet', value style.color = '#4CAF50'
       - If d.posOpen is false: value textContent = 'Geschlossen', value style.color = '#FF6B6B'
       - Append both to the diagnostics container
       - Also call `updatePosToggleButton(d.posOpen)` here so button label is correct on admin menu open

    6. Extend `hideAdminMenu()` (line 567) to also call `hidePosCloseConfirm()` — prevents confirm overlay leaking across admin open/close cycles (Pitfall 3).

    7. Wire the POS toggle button click handler. Do NOT add #admin-btn-pos-toggle to the wireAdminButtons() map (it needs special handling per RESEARCH.md anti-pattern). Instead, add a standalone click handler:
       - Get #admin-btn-pos-toggle element
       - On click: if posOpenState is true, call showPosCloseConfirm() (show confirm before toggling)
       - If posOpenState is false, call window.kiosk.adminMenuAction('toggle-pos-open') directly (no confirm per D-03), then on resolution call updatePosToggleButton and set posOpenState

    8. Wire #pos-confirm-yes click handler:
       - Call window.kiosk.adminMenuAction('toggle-pos-open')
       - On resolution: call hidePosCloseConfirm(), call updatePosToggleButton(result.posOpen), set posOpenState = result.posOpen

    9. Wire #pos-confirm-cancel click handler:
       - Call hidePosCloseConfirm() — no state change

    10. Extend the existing Esc keydown handler (the nested overlay guard around line 1026). Add a check for #pos-close-confirm visibility BEFORE the existing checks for credentials/pin-change/update-config overlays:
        - If #pos-close-confirm is visible (display !== 'none'), call hidePosCloseConfirm() and return (do not cascade to admin close)

    11. Subscribe to onPosStateChanged IPC in the IPC subscriber block (around line 978):
        ```
        if (window.kiosk && window.kiosk.onPosStateChanged) {
          window.kiosk.onPosStateChanged(function(payload) {
            var posOpen = !!(payload && payload.posOpen !== false);
            applyPosState(posOpen);
            updatePosToggleButton(posOpen);
          });
        }
        ```

    12. Extend the existing onShowWelcome subscriber (around line 997) to re-apply posOpenState after welcome:show fires. After the existing showWelcome() call, add:
        `applyPosState(posOpenState);`
        This ensures the welcome layer always renders the correct open/closed state on every welcome cycle (session reset, cold boot) regardless of IPC timing (per Pitfall 2 from RESEARCH.md).
  </action>
  <verify>
    <automated>node --check src/host/host.js</automated>
  </verify>
  <acceptance_criteria>
    - src/host/host.js contains `var posOpenState = true;` as a module-level variable
    - src/host/host.js contains `function applyPosState(posOpen)` with `'POS derzeit geschlossen'` and `'Bitte Studio-Personal'` strings
    - src/host/host.js contains `function updatePosToggleButton(posOpen)` with `'POS schliessen'` and `'POS \u00f6ffnen'` strings
    - src/host/host.js contains `function showPosCloseConfirm()` and `function hidePosCloseConfirm()`
    - src/host/host.js contains `hidePosCloseConfirm()` call inside hideAdminMenu
    - src/host/host.js contains `adminMenuAction('toggle-pos-open')` for both confirm-yes and direct-open paths
    - src/host/host.js contains `onPosStateChanged` subscriber calling applyPosState and updatePosToggleButton
    - src/host/host.js contains `applyPosState(posOpenState)` inside or after the onShowWelcome subscriber
    - src/host/host.js renderDiagnostics contains `POS-Status` string
    - src/host/host.js Esc handler contains a check for pos-close-confirm visibility
    - `node --check src/host/host.js` exits 0
  </acceptance_criteria>
  <done>host.js has full POS state rendering (welcome closed-state with tap suppression), admin toggle button with asymmetric confirm, confirm overlay show/hide/Esc, diagnostics row, and onPosStateChanged IPC subscriber with welcome-show re-apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full Phase 09 POS open/close toggle feature</name>
  <files>src/host/host.html, src/host/host.css, src/host/host.js, src/main/main.js, src/main/updateGate.js</files>
  <action>
    Human verification of the complete Phase 09 POS open/close toggle feature.
    What was built: Plan 01 wired the main-process backbone (updateGate admin-closed-window trigger, toggle-pos-open IPC, posOpen diagnostics, startup broadcast, preload channel, 4 new tests). Plan 02 Tasks 1-2 built the host-side UI (toggle button, confirm overlay, welcome closed-state, IPC subscriber).
    Steps to verify:
    1. Launch the app: npm start
    2. Open admin menu (Ctrl+Shift+F12, enter PIN)
    3. Verify POS schliessen button visible in yellow after PIN andern, before Auto-Update einrichten
    4. Verify diagnostics header shows POS-Status Geoffnet in green
    5. Tap POS schliessen — confirm overlay appears with POS wirklich schliessen? title
    6. Tap Abbrechen — overlay closes, no state change
    7. Tap POS schliessen again, then Ja schliessen
    8. Verify button becomes POS offnen in green, diagnostics shows Geschlossen in red
    9. Close admin menu (X or Esc)
    10. Verify welcome shows POS derzeit geschlossen heading and Bitte Studio-Personal verstandigen subtext
    11. Tap welcome screen — nothing happens (tap suppressed)
    12. Reopen admin, tap POS offnen (green) — toggles immediately no confirm
    13. Close admin — welcome shows Zum Kassieren tippen, tap works
    14. Esc test: open admin, tap POS schliessen, press Esc — confirm closes, admin stays
    15. Persistence: close POS, quit app, relaunch — welcome shows closed state immediately
    16. Run node --test test/updateGate.test.js — all 11 tests pass
  </action>
  <verify>
    <automated>node --test test/updateGate.test.js</automated>
  </verify>
  <done>All 16 verification steps pass: toggle button renders in both states with correct colors, confirm overlay works for close only, welcome closed-state shows branded message with tap suppressed, state persists across restart, Esc handles nested overlay correctly, all 11 updateGate tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host renderer to main process | adminMenuAction('toggle-pos-open') IPC crosses from untrusted renderer to main |
| main process to host renderer | pos-state-changed broadcast carries posOpen boolean to renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Spoofing | Renderer sends toggle-pos-open without admin auth | mitigate | Existing `if (!adminMenuOpen)` guard at main.js line 759 rejects all admin-menu-action calls when admin menu is not PIN-verified open. No new mitigation needed. |
| T-09-06 | Tampering | Welcome screen pointer-events override via DevTools | accept | Kiosk mode disables DevTools. Physical access to the machine implies full control anyway. |
| T-09-07 | Denial of Service | Confirm overlay left visible after admin close | mitigate | hidePosCloseConfirm() called in hideAdminMenu() — cleanup guaranteed on every admin close path. |
</threat_model>

<verification>
1. All HTML element IDs present in host.html (admin-btn-pos-toggle, pos-close-confirm, pos-confirm-yes, pos-confirm-cancel)
2. All CSS classes present in host.css (bsk-btn--admin-action--caution, bsk-btn--admin-action--safe, bsk-welcome-subtext, bsk-confirm-body)
3. `node --check src/host/host.js` passes
4. host.js contains applyPosState, updatePosToggleButton, showPosCloseConfirm, hidePosCloseConfirm functions
5. host.js onPosStateChanged subscriber wired
6. host.js hideAdminMenu calls hidePosCloseConfirm
7. Human verification confirms full toggle flow works end-to-end
</verification>

<success_criteria>
- Admin menu POS toggle button renders correctly in both states with proper colors and labels
- Confirm overlay appears for close action only (asymmetric confirm per D-02/D-03)
- Welcome layer shows branded closed-state message when posOpen=false with tap suppressed
- Welcome layer restores normal state when posOpen=true
- State persists across app restart
- Diagnostics show POS-Status with color-coded value
- Esc key correctly handles nested confirm overlay before admin close
- All 11 updateGate tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-pos-open-close-toggle-with-update-window-gating/09-02-SUMMARY.md`
</output>
