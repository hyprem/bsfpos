---
context: milestone-definition
milestone: v1.1 Field-Operations Polish
workflow: /gsd-new-milestone
workflow_step: 8 of 11 (research decision)
status: paused
last_updated: 2026-04-14T11:42:02.699Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: DO NOT re-run `/gsd-new-milestone`** — Step 6 silently runs `phases clear --confirm` which DELETED ~100 v1.0 phase doc files in this session. The deletion was caught and restored via `git restore .planning/phases/` BEFORE any commit, but re-running the slash command would re-trigger it. PROJECT.md + STATE.md are already updated for v1.1 (commit `c60d0be`, pushed). Resume the workflow by executing Steps 8–11 INLINE in the new session. Read `.planning/todos/pending/2026-04-14-gsd-new-milestone-phases-clear-destructive.md` for the full incident report and recommended fixes.

**Do not proceed until this box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-invoking `/gsd-new-milestone` to resume | The slash command runs `phases clear --confirm` in Step 6 which destroys v1.0 phase docs that were never archived (manual reconciliation was used instead of `/gsd-complete-milestone`). | blocking | Resume by executing Steps 8–11 INLINE: define REQUIREMENTS.md manually, then spawn the `gsd-roadmapper` subagent directly via the Task tool. Do NOT call the orchestrator slash command again. |
| Trusting `phases clear --confirm` is non-destructive | The `--confirm` flag implies "I've confirmed I want this," not "operation is safe." There is no preview, no archive fallback, no reference check. | advisory | Always `git status` after running tooling that touches `.planning/phases/` and inspect for unexpected deletions before any commit. |

<current_state>
v1.1 milestone is fully scoped and committed at the project level. PROJECT.md has the new "Current Milestone: v1.1 Field-Operations Polish" section, STATE.md is reset to defining_requirements status, and commit `c60d0be` is pushed to origin. The `/gsd-new-milestone` workflow is paused mid-Step-8: the research decision was presented (recommendation: skip) but the user pivoted to context handoff before confirming. Resume by acknowledging the skip-research recommendation and proceeding to Step 9 (define REQUIREMENTS.md).

Parallel context: the user triggered an auto-update test on the kiosk (0.1.2 → 0.1.3) during this session. Status was unconfirmed when this handoff was written. The kiosk update is independent of the milestone definition work; ignore unless the user reports an issue.
</current_state>

<completed_work>

Workflow steps completed:
- Step 1: Load context — done
- Step 2: Gather milestone goals — done (Theme A locked)
- Step 3: Determine version — done (v1.1)
- Step 3.5: Verify milestone understanding — done (user confirmed "looks good")
- Step 4: Update PROJECT.md — done (commit `c60d0be`)
- Step 5: Update STATE.md — done (commit `c60d0be`)
- Step 6: Cleanup + commit — done with caveat (phases clear was destructive; restored before commit; commit `c60d0be` only contains PROJECT.md + STATE.md, NOT the deletions)
- Step 7: Init + resolve models — done
- Step 8: Research decision — partial (recommendation presented to user as "skip"; user did not explicitly confirm before pausing)

Other work completed in this session that is NOT part of /gsd-new-milestone:
- v1.0 milestone reconciliation + tagging (commit `403f860` + tag `v1.0`)
- v0.1.1 / v0.1.2 / v0.1.3 patch releases shipped to GitHub
- Made `hyprem/bsfpos` repo PUBLIC to fix the electron-updater private-repo bug
- Renamed v0.1.2 release assets manually (hot-fix for the filename mismatch bug)
- Quick task `260414-eu9` — descope NFC member identification
- Quick task `260414-iiv` — ship 0.1.3 patch (artifact rename + maintenance window flip)
- 9 v1.1 todos captured in `.planning/todos/pending/`
- 1 todo completed and moved to `.planning/todos/completed/` (filename mismatch)
- Auto-update path validated end-to-end against real GitHub Releases
- v1.0 kiosk visit Phase 4/5/6/7/8.1/8.4 rows mostly tested or marked N/A (see runbook for status)

</completed_work>

<remaining_work>

`/gsd-new-milestone` workflow steps still to do (resume here):

- **Step 8 (finish):** Acknowledge the skip-research recommendation. No subagent spawn needed.
- **Step 9: Define REQUIREMENTS.md.** Read the 7 in-scope todo files in `.planning/todos/pending/`. Propose category prefixes for REQ-IDs (suggested: LOCALE, UX, ADMIN, FIX, SALE, but the 7 todos may map cleanly to fewer or different categories — confirm with the user). Draft a full requirement list and present it inline. After approval, write `.planning/REQUIREMENTS.md` and commit.
- **Step 10: Create roadmap.** Spawn the `gsd-roadmapper` subagent via the Task tool with `subagent_type: gsd-roadmapper`. Pass it PROJECT.md, REQUIREMENTS.md, MILESTONES.md, and instructions to (a) continue phase numbering from 7 (NOT reset), (b) derive phases from v1.1 requirements only, (c) write ROADMAP.md and update STATE.md and REQUIREMENTS.md traceability immediately. After it returns, present the proposed roadmap inline for approval. Commit after approval.
- **Step 11: Done.** Display the final milestone-initialized banner and tell the user the next command is `/gsd-discuss-phase 7` (or whichever number is the first v1.1 phase).

</remaining_work>

<decisions_made>

- **v1.1 theme = "Field-Operations Polish"** (Theme A from the choice presented). 7 in-scope todos. No new ambition. Goal: mature, validated, hands-off kiosk before any new feature work.
- **Skip research at the milestone level.** Per-phase research can be enabled later via `/gsd-plan-phase --research` if a specific phase needs it. (Recommendation given; user did not explicitly object before pausing.)
- **Continue phase numbering from 7.** v1.0 ended at phase 6. v1.1 phases will be 07, 08, etc. Do NOT pass `--reset-phase-numbers`. This avoids colliding with the v1.0 phase directories which are still in `.planning/phases/` (the previous milestone was completed manually via reconciliation, not via `/gsd-complete-milestone`, so phase dirs were never archived).
- **NFC member identification (todo #3) is deferred to v1.2+.** Out of v1.1 scope. Requires fresh permission/identification design (either a Magicline role with member-lookup permissions, or an alternative ID mechanism).
- **All 8 v1.1 todos minus #3 = the 7 in-scope features.** Bug fixes (#6 Kasse nachladen wedge, #9 Anmeldedaten mode bug) are folded into v1.1 alongside the features — no separate 0.1.x patches planned.
- **The GSD tooling note todo (`gsd-new-milestone-phases-clear-destructive`) is NOT v1.1 scope.** It's a meta-tooling fix for the GSD framework upstream. Don't include in REQUIREMENTS.md.

</decisions_made>

<blockers>

None. The workflow can be resumed cleanly inline. The only "blocker" is the meta-constraint above (do not re-run the slash command).

</blockers>

## Required Reading (in order)

1. **`.planning/HANDOFF.json`** — structured handoff with workflow state, decisions, in-scope todo list, anti-patterns
2. **`.planning/.continue-here.md`** — this file
3. **`.planning/PROJECT.md`** — read the new "Current Milestone: v1.1 Field-Operations Polish" section + the v1.1 entry under Active Requirements
4. **`.planning/STATE.md`** — confirm milestone is v1.1, status is `defining_requirements`
5. **`.planning/MILESTONES.md`** — v1.0 entry for context (what shipped + the post-ship NFC descope addendum)
6. **`.planning/todos/pending/2026-04-14-gsd-new-milestone-phases-clear-destructive.md`** — read this BEFORE you do anything else with `phases/` or `/gsd-new-milestone`
7. **The 7 in-scope todo files** (read in any order; they are the source for REQUIREMENTS.md):
   - `2026-04-14-lock-magicline-ui-to-de-de-regardless-of-windows-language.md`
   - `2026-04-14-keep-splash-visible-until-auto-selection-completes.md`
   - `2026-04-14-admin-pos-open-close-toggle-with-update-window-gating.md`
   - `2026-04-14-admin-menu-close-button.md`
   - `2026-04-14-kasse-nachladen-from-welcome-leaves-kiosk-stuck.md`
   - `2026-04-14-post-sale-vielen-dank-overlay-with-print-interception.md`
   - `2026-04-14-anmeldedaten-andern-shows-first-run-mode.md`

Do NOT include todo `2026-04-14-reintroduce-nfc-member-identification.md` — deferred to v1.2.
Do NOT include todo `2026-04-14-gsd-new-milestone-phases-clear-destructive.md` — meta-tooling, not project scope.

## Critical Anti-Patterns (do NOT repeat these)

- **Re-running `/gsd-new-milestone`** — destroys v1.0 phase docs via Step 6 `phases clear`. Resume the workflow inline.
- **Re-doing the research decision** — the user has effectively skipped research by pausing without objecting. Do not spawn 4 research agents on resume; go directly to requirements.
- **Re-asking the milestone scope question** — already locked as Theme A. The 7 in-scope todos are the answer. Confirming categories for REQ-IDs is the only remaining scope question.
- **Including the NFC reintroduction todo or the GSD tooling note in v1.1 requirements** — both are explicitly out of scope.
- **Trying to archive v1.0 phase dirs to `.planning/milestones/v1.0-phases/`** — DON'T. The phase dirs are intentionally left in place because the runbook (`docs/runbook/v1.0-KIOSK-VISIT.md`) actively references them. Leave the directory mixed-milestone (v1.0 phases 1-6 + future v1.1 phases 7+) until a future cleanup decides on a coexistence strategy.

## Infrastructure State

- **GitHub repo:** `hyprem/bsfpos` — **public** (changed from private earlier today to fix electron-updater private-repo bug). Auto-update path validated end-to-end.
- **Latest released version:** v0.1.3 (artifact filename fix + maintenance window 09–12 flip). Auto-update from v0.1.2 → v0.1.3 was triggered on the kiosk in parallel with this handoff but not confirmed at handoff time.
- **v1.0 git tag:** unchanged on commit `403f860`. Do NOT modify.
- **Current HEAD:** `139156b` (after handoff commit, will become whatever this commit's hash is)
- **Branch:** `master`, tracking `origin/master`. After this handoff commit, push so the next session has it remotely.
- **Working tree (before this commit):** clean. v1.0 phase dirs intact (restored from accidental delete).
- **`.planning/phases/`:** contains v1.0 phase directories (01-06) — leave them alone.
- **`.planning/milestones/`:** contains v1.0-ROADMAP.md, v1.0-REQUIREMENTS.md, v1.0-MILESTONE-AUDIT.md (manually reconciled).
- **`.planning/todos/pending/`:** 9 files (8 v1.1 candidates + 1 GSD tooling note). The v1.1 candidates are the 7 in-scope above + the deferred NFC reintroduction.
- **`.planning/todos/completed/`:** 1 file (the filename mismatch fix shipped in 0.1.3).
- **`.planning/quick/`:** 2 directories (260414-eu9 NFC descope + 260414-iiv 0.1.3 patch). Both completed.

## Pre-Execution Critique Required

Not applicable — this pause is between requirements gathering and roadmap creation, not between design and execution. Skip.

<context>

The session ran from v1.0 milestone reconciliation through to the v1.1 milestone definition. We shipped 3 patches (0.1.1, 0.1.2, 0.1.3) end-to-end via auto-update to validate the auto-update story. The kiosk survived 10 welcome cycles cleanly during the §7 Phase 6 smoke test (the original 2026-04-12 third-cycle bug is dead). 8 bugs/findings were captured as v1.1 todos throughout the visit. The user explicitly chose Theme A (polish only, no new features) for v1.1 scope to give the kiosk a stable validated baseline before any expansion.

The mental state when pausing: "milestone is scoped, just need to formalize requirements + roadmap. The 7 todos are detailed enough that REQUIREMENTS.md can be drafted in one pass. Roadmap should split them into 2-4 phases by area (UX/host overlays vs main-process bug fixes vs admin menu features vs new feature work like print interception). The user is busy with kiosk auto-update test in parallel and wanted to /clear before context fills up — so the handoff needs to be self-sufficient for a fresh agent."

The user is technical but values brevity in agent responses. They use German throughout the kiosk UI and correct UX details matter. They want the field-validated v1.0 in production, then v1.1 polish, then maybe v1.2 with NFC reintroduction.

</context>

<next_action>

After /clear and `/gsd-resume-work`, execute these steps in order:

1. **Read all files in the Required Reading list above.** Especially `.planning/HANDOFF.json` and the 7 in-scope todos.
2. **Acknowledge the BLOCKING CONSTRAINT** at the top of this file by stating in your first user-visible message: *"I will not re-run /gsd-new-milestone. Resuming Steps 8–11 inline."*
3. **Confirm with the user** that research is being skipped and ask them to choose category prefixes for REQ-IDs (recommend something simple based on the actual 7 todos — e.g., LOCALE, UX, ADMIN, FIX, SALE — but only after reading the todos to see what categorization actually fits).
4. **Draft REQUIREMENTS.md** based on the 7 in-scope todos. Each requirement should be specific, testable, user-centric, and atomic per the workflow guidance. Group by category. Continue REQ-ID numbering from where v1.0 left off if the prefix overlaps (none of the v1.0 prefixes overlap with the suggested v1.1 prefixes, so all v1.1 IDs start at 01).
5. **Present the requirements list inline** for user approval. Iterate if "adjust."
6. **Commit:** `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs: define milestone v1.1 requirements" --files .planning/REQUIREMENTS.md`
7. **Spawn the gsd-roadmapper subagent** via the Task tool. Pass it PROJECT.md, REQUIREMENTS.md, MILESTONES.md. Instructions: continue phase numbering from 7, derive phases from v1.1 requirements only, write ROADMAP.md + STATE.md + REQUIREMENTS.md traceability immediately.
8. **Present the proposed roadmap inline** for user approval. Iterate if "adjust."
9. **Commit:** `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs: create milestone v1.1 roadmap (N phases)" --files .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md`
10. **Display the final milestone-initialized banner** and tell the user the next command is `/gsd-discuss-phase 7` (or whichever number ended up being the first v1.1 phase).
11. **Push:** `git push`. Then the v1.1 milestone is fully scoped and ready for execution.

Stop here. The user takes over with `/gsd-discuss-phase 7` (or `/gsd-plan-phase 7` to skip discussion).

</next_action>
